# ARCHIVE INDEX — AI Chauffeur capabilities · 2026-09-25

One row per capability found in the RUN A export. **Where it lives** names the exact agent version,
flow node id, or workflow + node. **Re-enable** says what brings it back if a later run turns it off.
All rows were verified on **2026-09-25** by reading the live API (Retell agent v27 / flow v27,
n8n active versions listed in `ROLLBACK-BUNDLE-2026-09-25.md` §4), not from earlier reports.

Paths: Retell exports are in `retell/2026-09-25/`, n8n exports are in `n8n/2026-09-25/`.

| # | Capability | What it does | Where it lives | How to re-enable | Last verified |
|---|---|---|---|---|---|
| 1 | **Rate engine RCv2.x** | Prices a trip on the call. The ladder is exact zone → route distance/time → hourly minimum → geo fallback → human review. Speaks an all-in number with a 20% "service fee" (the word "gratuity" is retired). Writes a `quote_audit` row per quote. | n8n **WF-RATE** `2JlTkvQ1dGiwwjw9` (code constant `RATE_CARD_VERSION = 'RCv2.3'`, node *Compute Quote*). It reads 7 Data Tables (rows stamped RCv1.0, tenant `demo`). Called by flow node **N06 Rate Engine** `n06_rate_engine` (tool `tool_rate_lookup`, inside component `conversation_flow_component_bd1ebd42f46d`). Auth header `x-rate-secret` = `$vars.RATE_SHARED_SECRET`. | Keep WF-RATE published at `aa76fa61-…`, keep the `rate_lookup` tool on the flow, and keep flow default `pricing_mode=RATE_CARD`. Table rows: `datatable.*.json` (R7). | 2026-09-25 |
| 2 | **Capacity matrix (N05)** | Checks passengers and luggage against the vehicle class before pricing. Routes oversized parties to a recommendation, or to a warm handoff. | Flow component **N05-N06 Rate and Capacity Pipeline** `conversation_flow_component_bd1ebd42f46d` → code node **N05 Capacity Matrix** `n05_capacity`, then branch **N07 Capacity Check** `n07_capacity_check` → **N07 Capacity Mismatch** `n07_capacity_mismatch` / **Exit - Oversized (N05)** `exit_oversized` → **N09 Warm Handoff - Oversized** `n09_warm_handoff`. Input guard **N05B** `n05b_rate_guard`. | The component is local to flow v27. Restore via R1/R2, or rebuild the flow from JSON (R6). | 2026-09-25 |
| 3 | **Terminal Suite email** | The HTML trip ticket ("AI CHAUFFEUR — THE TERMINAL SUITE v4") emailed after each call: to the owner, optionally to a prospect, and to the caller when an email was captured and a trip exists. | Rail `TkETvvnABhUPd7ME`, node **Build Trip Ticket** (renders the HTML), sent by **Email Ticket to Owner**, **Email Ticket to Todd** (gated by *Todd Copy Gate*) and **Email Copy to Caller** (gated by *Caller Copy Gate*). Gmail credential `75yoiG46HfIX5VkQ`. | Rail active version `0652d902-…` (R5). The owner recipient is hardcoded in the node and redacted in the archive, so re-enter it on import. | 2026-09-25 |
| 4 | **Child-seat array** | Captures up to three child seats with a type for each, and prints them on the ticket. | Flow `default_dynamic_variables` `child_seat_1_type` / `_2_` / `_3_`. Asked in **N04B Additional Details** `n04b_additional_details`, extracted in **N04B Verify Details** `n04b_verify` (`child_seats_needed`, `child_seat_1_type`…`_3_type`). `child_seats_needed` is first captured by **N04 Completeness Gate** `n04_gate`, which routes to N04B. Read back in **N06R** `n06r_readback`. Rendered by rail *Build Trip Ticket* (`child_seat` keys). | Flow v27 (R1/R2) + rail `0652d902-…`. | 2026-09-25 |
| 5 | **Gate codes / additional details (N04B)** | Collects gate codes, special notes and other pickup details before readback. | **N04B Additional Details** `n04b_additional_details` → **N04B Verify Details** `n04b_verify` (extract vars `gate_code`, `special_notes`). Also referenced in **N03A Arrival - Collect** `n03a_arrival_collect` and **N06R Readback** `n06r_readback`. Rail *Build Trip Ticket* prints `gate_code`. | Flow v27 (R1/R2). | 2026-09-25 |
| 6 | **ANI greeting (N00)** | Looks up the caller's number before the first word, so a known caller is greeted by name and their last trip is on hand (`caller_known`, `last_trip_*`). | Flow **start node** `n00_ani_lookup` (tool `tool_ani_lookup`, 2500 ms timeout) → **N01 Greeting** `n01_greeting`. **Served by WF-ANI — AI Chauffeur `XLSd3vt41vhsXIA9`** at `/webhook/tools/ani-lookup`, **not** by WF-ANI-AVA `ITGRwcRKxLgmKnBZ` (that one serves the 8930 sales line at `/webhook/ava-inbound-lookup`). Both are archived. | Keep `XLSd3vt41vhsXIA9` published at `5555fbf6-…`, and keep `start_node_id = n00_ani_lookup`. | 2026-09-25 |
| 7 | **Booking commit** | Writes the confirmed reservation. Idempotent: a repeat call returns the stored result, verified by read-after-write hash compare. | Flow **N08 Prep Commit** `n08_prep_commit` → **N08 Commit** `n08_commit` (tool `tool_commit_reservation`, 15000 ms) → n8n **WF-COMMIT** `O1fX0FpbT0qqMnqJ` `/webhook/tools/commit-reservation`, with Data Table `reservation_commits`. Last 30 executions: all CONFIRMED (7 early-respond, 23 duplicate/idempotent). | WF-COMMIT `61c1f22b-…` (R5) + flow v27. | 2026-09-25 |
| 8 | **Email capture (CC03)** | Asks for the caller's email, reads it back in plain letters and saves it normalised. It feeds the caller copy of the ticket. | Component **Contact Capture** `conversation_flow_component_32205ff20393` → **CC03 Email Capture + Confirm** `cc_ask_email`, verified in **CC04** `cc_verify`. Entered from 4 places: `cc_before_commit`, `cc_before_escalation`, `cc_before_held_or_request`, `cc_before_no_transaction`. | Flow v27 (R1/R2). | 2026-09-25 |
| 9 | **Corporate / personal question (N02)** | Sorts the caller as retail or corporate. That picks the standard or corporate quote node. | **N02 Intent Router** `n02_router` extracts `account_type` ("retail or corporate"). Default `account_type=retail`. It splits at **N07 Quote - Standard** `n07_quote_standard` / **N07 Quote - Corporate** `n07_quote_corporate`. | Flow v27 (R1/R2). | 2026-09-25 |
| 10 | **Reliable tenant** | A second operator demo on its own line. It captures only and never quotes. Tickets are prefixed `REL-`. | Agent `agent_367be6cf3c722e89fca03e34b5` v29 → flow `conversation_flow_36c260d873de` v29 (components *Airport Trip Intake* `…93a81b07f692`, *Ground Service Intake* `…8e2869a96377`). Number +1 414-409-5008. Rail `TENANTS` row `CAPTURE-ONLY` + prefix rule `REL`. `DEMO_RECIPIENTS` entry is empty. Flow v29 has **no tools**. v19–v26 carried a `book_appointment` tool that is gone in v29. | R1 on 409-5008 with v29. The rail row lives in *Build Trip Ticket*. | 2026-09-25 |
| 11 | **Contact Capture / Closing components** | Capture name → phone → email before every exit. Closing asks for demo feedback, offers a callback and signs off. | Local components `…32205ff20393` (CC01–CC04) and `…fc58ffdd5d9c` (*Closing - Demo Feedback*, *Offer Callback*, *Signature*), plus *Closing - No Transaction* branch `closing_before_no_transaction` → pitch or operational close. | Flow v27. | 2026-09-25 |
| 12 | **Global safety nodes** | Can fire from anywhere in the call: correction, cancel, quote failure, write failure, modify existing trip, desk interrupts. | Global nodes `g_correction`, `g_cancel`, `g_quote_failure`, `g_write_failure`, `g_modify_existing`, `g_desk_interrupts`. | Flow v27. | 2026-09-25 |
| 13 | **Outbound callback greeting** | A separate greeting when AI Chauffeur calls a caller back. | **N01 Greeting - Outbound Callback** `n01_greeting_outbound`. The outbound agent on 775-0019 is the same agent. | Flow v27. | 2026-09-25 |
| 14 | **Post-call owner + caller SMS** | Texts the owner a status-led alert, and texts the caller their ticket when consent and a trip exist. | Rail **Owner SMS Alert** (`$vars.OWNER_ALERT_CONTACT_ID`, `$vars.OWNER_SMS_FROM`), **Ensure Owner Contact** (`$vars.OWNER_CELL`), **Upsert Caller in GHL** → **SMS Ticket to Caller** (gated `sms_ok`). | Rail `0652d902-…`. See the gotcha below on the caller upsert. | 2026-09-25 |
| 15 | **Browser demo (/try)** | One-button web call to the live agent, capped at 8 min, with a proof-of-work gate and a receipt endpoint. | WF-TRY-WEBCALL `9nKn8i2dRuALikuv` → `agent_2d1d…` `latest_published`. | WF-TRY-WEBCALL `89829d66-…`. Kill switch: unpublish. | 2026-09-25 |
| 16 | **Unwired placeholder nodes** | Nine default-named nodes with no inbound edge. They cannot run: *Conversation*, three *Subagent*, *Press Digit*, *Logic Split*, *Agent Transfer Node* (no target agent), *In-Call SMS*, *Extract Variables*. | Flow v27 ids `node-1788662623208` … `node-1788662644981`. | Nothing to re-enable. Listed so a later run does not mistake them for features. | 2026-09-25 |

**Gotcha on row 14, found in RUN A:** rail execution **10520** (reference call
`call_2b7983575f5c6ce251ab4049dd9`, 2026-09-24) upserted an **existing** GHL contact
(`new:false`). It overwrote first/last name with `Mike` / `(AI Chauffeur demo)`, replacing the name
a real person had typed into the aichauffeur.ai/book widget on 2026-08-17. The upsert writes a
name whether or not the contact already has one.
