# ============================================================================
# aichauffeur.ai — robots
# ----------------------------------------------------------------------------
# AIC SITE RUN 1 · TASK D
#
# This file is served at https://aichauffeur.ai/robots.txt because the
# `aichauffeur` Vercel project has Root Directory = /chauffeur/. It is a REAL
# FILE, not a vercel.json rewrite: the repo-root vercel.json is never read by
# that project, so the host-conditional rewrite approach cannot work here.
# Before this run, aichauffeur.ai/robots.txt returned 404.
#
# aivoiceagency.ai has its own robots.txt at the repo root. The two hosts are
# separate sites and must never cross-index.
# ============================================================================

User-agent: *
Allow: /

# RUN 10 · DESIGN-SYSTEM.md is the brand's engineering law and lives at the
# chauffeur root because that is where a future run will look for it. RUN 9 left
# it in the deploy bundle and blocked it here instead; chauffeur/.vercelignore
# now excludes it, so the path 404s rather than merely asking crawlers not to
# look. This line stays because a Disallow on a path that no longer exists costs
# nothing and a crawler that already has the URL will stop asking for it.
Disallow: /DESIGN-SYSTEM.md

# RUN 10 · llms.txt is a description of this site for answer engines. It is meant
# to be fetched, so it is deliberately NOT disallowed.

Sitemap: https://aichauffeur.ai/sitemap.xml
