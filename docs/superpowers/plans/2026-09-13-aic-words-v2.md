# AI Chauffeur · Words Final v2 (Hero B) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to run this plan task by task. Steps use checkbox (`- [ ]`) syntax.

**Status:** PLAN, stopped for GO · 2026-09-13 · written against `main` @ `4a12037` (= `origin/main`). Nothing on aichauffeur.ai has changed.

**Goal:** Put Hero B and the new below-the-fold section on the aichauffeur.ai homepage, ship `/what-it-does/` and `/what-it-can-do/`, turn the green PUSH TO BOOK button into one shared class, retire the AVA name and `/demo/` on aichauffeur.ai, and amend the brand law and its gates to match.

**Architecture:** Static pages in `chauffeur/`, which is its own Vercel project (repo-root paths 404 there). One shared `.btn-go` modifier in `chauffeur/assets/aic.css` replaces the inline v7 button rules on `/try/`. Chrome (nav, drawer, footer, CTA stack) is hand-copied into every page, so each chrome change lands on all 19 existing pages plus the two new ones. The brand law is `chauffeur/DESIGN-SYSTEM.md`; the gate scripts in `tools/` enforce it and are updated in the same run.

**Tech stack:** vanilla HTML/CSS/JS · Node gates (`tools/aic-run9-gate.mjs`, `aic-run10-gate.mjs`, `aic-run11-gate.mjs`, `aic-run12-gate.mjs`, `aic-run13-parity.mjs`) · Python (`tools/stamp_chauffeur.py`, `tools/aic-run10-faq-mirror.py`) · n8n and Retell **read-only**, except the one approved test call in Task 0.

## Global Constraints

- **Host:** aichauffeur.ai = Vercel project rooted at `chauffeur/`. Deploy = push to `main`. No build step.
- **CSS homes:** component rules in `chauffeur/assets/aic.css`, document rules in `chauffeur/assets/circulant.css`. `chauffeur/index.html` never grows a `<style>` element (gated).
- **Palette:** Signal blue `#3D7BFF` (text, links, lines, proof-row numbers) · action blue `#1E56D6` (blue fills) · Booked-Green `#2EE6A8` (money buttons and success states). `#00D4FF` only inside `chauffeur/assets/brand/` and the root favicon files.
- **Type:** Space Grotesk and JetBrains Mono only. 12px floor.
- **Phone:** display `(414) 775-0019`, link `tel:+14147750019`. `414-240-8930` and `350-220-5305` never appear.
- **Name:** "AI Chauffeur" is the only name for the agent on aichauffeur.ai. `\bAVA\b` = 0 in shipped files. aivoiceagency.ai is not touched.
- **No-name law:** `shane` = 0 and `founder` = 0 in every shipped `chauffeur/` file, comments included. Source comments say "ruling 2026-09-13", never a person.
- **Banned in copy:** "sounds human", "100% accurate", "nobody", "seamless", "revolutionary", "memory", "locked". "Confirm/confirmed" only with dispatch.
- **COPY LAW:** never lead with after hours / nights / weekends on the homepage, `/what-it-does/`, `/what-it-can-do/`, `/try/`, the PDF, or email.
- **No prices** on the homepage, the two new pages, and `/try/`. `/rates/` keeps its rate card.
- **URLs:** trailing slash everywhere; every indexable page has a self canonical.
- **/try/:** stays `noindex,follow`. Its call-start module script is not edited (Sep 30 migration run).
- **Voice lane:** no Retell or n8n writes, no agent prompt edits (CLAUDE.md PROMPT AUTHORITY LOCK).
- **Readability:** grade 5–7. Titles ≤60 characters, descriptions ≤155.
- **Rhythm:** sections 96px desktop / 56px mobile (`DESIGN-SYSTEM.md` § 9).
- **Git:** stage explicit paths only, never `git add -A`. ≤5 pushes an hour. After any change to `aic.css`, `aic.js` or `circulant.css`, run `python tools/stamp_chauffeur.py`; never run `tools/stamp.py` on this run.
- **Board:** `hq/board.json` is CRLF with hand escaping — edit it as text, never through `json.dumps`.

## 0 · How this run works

1. Read `CLAUDE.md`, then `chauffeur/DESIGN-SYSTEM.md`, then this file.
2. Loadout (CLAUDE.md § SKILL ROUTER): `chauffeur-design` + `frontend-design` + `ui-ux-pro-max` + `taste`, and `verification-before-completion` before every commit. The `chauffeur-design` skill still describes the old cyan site; `DESIGN-SYSTEM.md` wins on this host.
3. Order: PLAN (this file) → **GO** → Task 0 (customer-text proof call) → Tasks 1–N → checks on production → publish → DONE table → the critique, after DONE and not before.
4. Anything not live at the end: first line `RUN INCOMPLETE — what / why / next`.

## 1 · Rulings — Sep 13 2026

| # | Ruling |
|---|---|
| 1 | **Accent:** Signal blue stays (`#3D7BFF` / `#1E56D6`, as built in RUN 9). No cyan on aichauffeur.ai. Proof-row numbers are Signal blue. |
| 2 | **Quoting:** "quotes it" ships as written; the copy is not softened. Making the demo quote reliably is voice-lane work (Conductor Round A, item A1: the rate tool's `message` spoken verbatim), not this run. DONE's GOTCHAS reports the demo's real quoting record. *PLAN found the premise is off: agent v27 has the rate tool wired and quoted once in 7 days, by phone; no browser demo reached the price step. See § 3 C15 and § 9 D1.* |
| 3 | **CRM:** "puts it in your CRM" stands; "before the call is over" is dropped. This supersedes the claim ladder in `DESIGN-SYSTEM.md` on this point: it writes to the CRM (GHL) today. Never name a CRM product. |
| 4 | **Twenty calls:** the number stays. Source: Retell account concurrency, 20 base, more purchasable per client — provisioned capacity for a client desk, not the demo's cap. A source comment goes in the HTML. The `/try/` cap (refuse at 10 of 20 slots, 40 a day, 3 per IP per hour) is unchanged. |
| 5 | **After-hours page:** `/after-hours-limo-dispatch/` is a search page. Its URL, sitemap slot and nav/footer/drawer links stay. The homepage body links to it stay; the sentences around them drop nights-and-weekends framing and say "the calls your office can't get to." |
| 6 | **Hero parts:** keep the call-me form, `data-rail-after`, and the Dispatch Console. Replace only the eyebrow, H1, sub, buttons, and the lines under them. The console is re-copied (§ 4). |
| 7 | **Naming:** AI Chauffeur answers. AVA does not exist on aichauffeur.ai — every instance on every page, the Google section included (its structure and claims stay word for word; only the name changes). aivoiceagency.ai is untouched. |
| 8 | **Fold:** the header phone chip becomes a green OUTLINE on every page; the hero PUSH TO BOOK is the only green FILL on the fold. Green means money on this site. The fold gate counts green fills, not blue. |

## 2 · Corrections to brief v1

- **Law patch → `chauffeur/DESIGN-SYSTEM.md`, not CLAUDE.md.**
  - STATE LAW (§ 2, line 139): green also marks money buttons (agent and booking CTAs).
  - GLOW LAW (§ 4, line 551): one exception — the money-button rim glow. Light only; **the button never scales.** Written that way so no later run "fixes" v7, whose glow radius does breathe.
  - Primary control (§ 3, line 277): PUSH TO BOOK is ALL-CAPS by ruling.
  - CTA CANON (§ 7, line 623): "The demo is the button at /try, or the phone."
  - CLAUDE.md gets one pointer line to these rulings.
- **Gates:** `tools/aic-run11-gate.mjs:542-551` allows the green money-button glow; the fold probe counts green fills, not blue; `tools/aic-run12-gate.mjs:467` takes the new URL count.
- **`.btn-go`:** a byte-for-byte lift of the v7 rules (`chauffeur/try/index.html:213-274`), hover-sliver fix included (comment `:267-273`, rule `:274`). No `.is-live`: states hang off `data-state` exactly as v7 does. The homepage button has no live state — it is a link to `/try/`. Parity is proved with animations frozen, via `tools/aic-run13-parity.mjs`.
- **"Every one booked" stays.** "Books it" is set wording. The v1 ban narrows to "confirmed": confirmation language stays "with dispatch"; booking language is free.
- **"Hands the call to your on-call"** comes off the homepage six and `/what-it-does/`. It is built-to-order only (`/what-it-can-do/`). The sixth homepage bullet is **Every call recorded.** The recording and every word of the call come back with the trip sheet.
- **"18 vehicles":** source = the rate-engine `vehicle_classes` table (18 rows, RCv2.2). `/rates/` shows 15 and is stale; it is not touched this run, and GOTCHAS says so. **"Anywhere in the U.S."** = RCv2.2 layer 2 `ROUTE_DISTANCE_TIME` (any U.S. route).
- **"Text to the customer":** proved in Task 0, not asserted in copy. One real `/try/` call with Shane's own number as the customer number (`tools/aic-try/call.mjs`). If the customer text arrives, the claim stands as written. If not, the line reads "text to you, email to the customer" and GOTCHAS says so.
- **"No prices"** is scoped to the homepage, the two new pages, and `/try/`. `/rates/` keeps its rate card.
- **`/demo/` → `/try/`:** 308 is fine. Every in-site link, `llms.txt`, and the sitemap change in the same run.
- **"Every page"** = all 19. Nav and footer are hand-copied; do all 19. New links go in the Product menu and in the footer's first column.
- **Phone** `(414) 775-0019` everywhere. Trailing-slash URLs. Events: `hero_push_to_book`, `nav_call`, `nav_book`, `wid_push_to_book`, `wicd_push_to_book`.
- **New pages** follow the answer-page law (`DESIGN-SYSTEM.md` § 3.6, lines 482-500): 40–60-word answer first, one table, last-updated stamp, four FAQs. Titles ≤60 characters, descriptions ≤155.

> **PLAN note:** three corrections above rest on premises the evidence changes — "RCv2.2" (§ 3 C2), "$145 sedan transfer minimum" (§ 3 C4), and the owner's-number test (§ 9 D7). The rulings stand; only the facts under them are corrected.

## 3 · Sources — one per number and claim (read-only, 2026-09-13)

| # | Claim · where | Verdict | Source | Consequence |
|---|---|---|---|---|
| C1 | "Twenty calls at once" · H1, bullet 1, proof row, `/what-it-does/` | SOURCED | Retell `GET /get-concurrency`: limit 20, base 20, purchased 0, more purchasable · `automation/try-webcall/README.md:35` · WF-TRY-WEBCALL node "Capacity check" | Ships. Account-wide today: every line and `/try/` share the 20. HTML source comment. |
| C2 | "18 kinds of vehicles, sedan to 56-passenger coach" | SOURCED | `automation/rate-engine/wf-rate.02-compute-quote.js:23-42` (18 priced classes; `motorcoach_56` at `:40`) · live card `RCv2.3` (`:3`) · n8n `vehicle_classes` `dofpCkfUf9u13OCt`, 18 rows (tenant `demo`) | Ships. Source note says RCv2.3, not RCv2.2. Demo rates are a sample model (`:20`). |
| C3 | "anywhere in the U.S." · "prices it by the miles and the drive time" | SOURCED (engine) | `wf-rate.02-compute-quote.js:15`, `:485-490` · WF-RATE `2JlTkvQ1dGiwwjw9` published `aa76fa61` · execution 9120 | Ships. From a live call only airport legs reach this path today (flow v27). |
| C4 | "$145" · console | CONTRADICTED for that trip | `:24` is the sedan floor, applied only on the miles path (`:490`); the zone path has no floor (`:442-458`); downtown Milwaukee → MKE by sedan = **$78** (zone row base 65 + 20% service; `quote_audit` row 42) | § 9 D2 |
| C5 | "Hourly minimums, late-night, same-day, and holiday rates" | SOURCED | `:44-45` (×1.15 / ×1.15 / ×1.20), `:401` · execution 9031 (Labor Day ×1.2) | Ships. |
| C6 | "One clean number, service fee included" | SOURCED | spoken on `call_4f3a135d470f71ef40f58d0b750` (2026-09-06): "All in, that's four hundred sixty dollars, service fee included." | Ships. |
| C7 | "If there's no card on file, it sends a secure link to pay" | CONTRADICTED | no card check behind the line; the text links to `/reserve/`, whose card block is `disabled` — "No card, no charge" (`chauffeur/reserve/index.html:8`, `:440-444`); no payment processor exists | § 9 D3 |
| C8 | "The trip sheet is emailed to you … every stop, every note" | SOURCED except stops | demo rail `TkETvvnABhUPd7ME` published `0652d902`, node "Email Ticket to Owner": 20 of 20 since 09-06; the ticket has no stops line | § 9 D5 |
| C9 | "A text alert hits your phone" · proof row "every trip sheet texted" | SOURCED | rail node "Owner SMS Alert": GHL message id 20 of 20 | Ships. Sent from the parent brand's text line (§ 10). |
| C10 | "The customer gets their trip by text and email" | SOURCED — internal tests only | rail nodes "SMS Ticket to Caller" / "Email Copy to Caller": 3 of 20 runs (9123, 9126, 9145), all one internal contact; 0 outside callers; 0 of 12 `/try/` calls | § 9 D7 |
| C11 | "puts it in your CRM" | SOURCED on the phone line · not on `/try/` | WF-COMMIT `O1fX0FpbT0qqMnqJ` v`61c1f22b` (GHL contact + reservation record + note; ledger `DCNqBPRtXLaLFSEh`, 31 rows) · rail node "Upsert Caller in GHL" | Ships (ruling 3). GOTCHA: the browser demo writes no CRM record. |
| C12 | "more people or bags than the car holds … moves the trip up" | HALF TRUE | people: execution 9033 re-quoted to a Mini Coach · bags: `:322` reads `vc.cap` but the column is `capacity`, so the check never fires; 12 passengers + 16 bags stayed in a Sprinter | § 9 D4 |
| C13 | "flagged for review before dispatch confirms it" | PARTLY TRUE | rail "REVIEW REQUIRED" (execution 9442) goes out after the call; WF-COMMIT returns CONFIRMED and the agent says "You're all set" first | § 9 D6 |
| C14 | "the recording and every word of the call" | SOURCED | owner email "The call" + "Listen to the call" (execution 9123) · `/try/` receipt | Ships (a link and inline text, not file attachments). |
| C15 | "quotes your rates" · "Your rates, quoted live" | SOURCED on the phone line · never on `/try/` | agent `agent_2d1d687eb85e6d5d0e720795c2` v27, "Pricing mode: RATE_CARD"; WF-RATE runs since 09-06: 1 (execution 9120); the 12 `/try/` calls lasted 4–60 s | Ships (ruling 2). § 9 D1 for the GOTCHA wording. `schema/aic-reservation-v1.json:120-128` still says CAPTURE_ONLY and is stale. |
| C16 | the agent's name on a call | CONFLICT (voice lane) | greeting "Thank you for calling AI Chauffeur"; prompt identity "You are AVA"; callback opener "this is AVA calling back" | § 10 |
| C17 | `/what-it-can-do/` options | MIXED | second brand: SOURCED (Reliable demo line, own agent) · account type lookup: SOURCED (WF-ANI `billing_type`) · repeat callers: BUILT, NOT WIRED (0019 has no inbound webhook) · live transfer, Spanish, card lookup, driver on duty, affiliate farm-in: NOT FOUND | § 9 D8 |

## 4 · Copy — final, inline

### 4.1 Homepage hero

| Slot | Copy |
|---|---|
| Eyebrow | `FOR LIMOUSINE AND BLACK CAR COMPANIES` |
| H1 | `Twenty calls at once. Every one answered. Every one booked.` |
| Sub | `AI Chauffeur answers your phones and books the trips. It takes the whole trip like your best dispatcher — flight number, meet and greet, passengers, bags, child seats, gate codes. It quotes your rates. It books it. It puts it in your CRM.` |
| Position line (own row, Space Grotesk, larger than the sub, semibold) | `Your people run the cars. AI Chauffeur runs the phones.` |
| Button (green fill) | `PUSH TO BOOK` → `/try/` · `data-event="hero_push_to_book"` |
| Under it | `Try it in your browser. Not a phone call. It talks to you right here.` |
| Second line | `Or hear it by phone: (414) 775-0019` (tel link) |

### 4.2 Homepage section below the fold

H2: `Built for the day the phones blow up. Not a chatbot.`

1. **Twenty calls at once.** Every one answered by name. Every one worked to a booked trip. Your phone stops going to voicemail.
2. **The whole trip, like a dispatcher.** Pickup, drop, date, time, flight number, curbside or meet and greet, passengers, bags, child seats, extra stops, gate codes, notes for the chauffeur.
3. **Your rates, quoted live.** It knows your rate sheet. Eighteen kinds of vehicles, sedan to 56-passenger coach, anywhere in the U.S. One clean number, service fee included.
4. **Booked. In your CRM.** The trip is written where your dispatcher works.
5. **Trip sheet on your phone.** Text to you, text and email to the customer, the recording and every word of the call attached.
6. **Every call recorded.** The recording and every word of the call come back with the trip sheet.

Proof row (Signal blue): `20 calls at once` · `18 vehicles quoted, anywhere in the U.S.` · `Every call recorded, every trip sheet texted`

Close: `Put it on every reservation call, or only the calls your office can't get to. Push the button and hear it work.` → green PUSH TO BOOK → `/try/`

Links: `See everything it does` → `/what-it-does/` · `See what it can do for your company` → `/what-it-can-do/`

### 4.3 `/what-it-does/`

- **Title:** `What AI Chauffeur Does | AI Chauffeur`
- **Description:** `AI Chauffeur answers the phones for limo and black car companies, takes the whole trip, quotes your rates, books it, and puts it in your CRM. Twenty calls at once.`
- **H1:** `What it does`
- **Answer first (52 words):** `AI Chauffeur answers the phones for limousine and black car companies and books the trips. Twenty calls at once, dispatch slammed — it picks up every one. It takes the whole trip the way your best dispatcher would, quotes your rates, books it, and puts it in your CRM. Every call is recorded.`
- **Table (the one table):** three columns — On the call · The quote · After the call — first four lines of each list below.
- **H2 On the call:**
  - Answers with your company name — every call, twenty at a time, around the clock.
  - Takes the whole trip: name and phone, date and time, pickup and drop-off, how many people, how many bags.
  - Airport runs: the airline and flight number, curbside or meet and greet at the terminal.
  - Picks the right car for the group. If there are more people or bags than the car holds, it moves the trip up to a bigger car.
  - Child seats — which kind and how many.
  - Extra stops along the way.
  - Notes for the chauffeur: gate codes, coffee with cream and sugar, champagne on ice, whatever the customer asks for.
- **H2 The quote:**
  - Quotes the trip live. It knows your rate sheet. If a trip isn't on your rate sheet, it prices it by the miles and the drive time.
  - Rates are loaded for 18 kinds of vehicles, from a sedan to a 56-passenger coach, for trips anywhere in the U.S.
  - Hourly minimums, late-night, same-day, and holiday rates are built in.
  - The customer hears one clean number, service fee included.
  - If there's no card on file, it sends a secure link to pay.
- **H2 After the call:**
  - The trip sheet is emailed to you: rate, vehicle, passengers and bags, every stop, every note.
  - A text alert hits your phone.
  - The customer gets their trip by text and email.
  - It puts the trip in your CRM.
  - Anything missing is flagged for review before dispatch confirms it.
  - Every call is recorded. The recording and the words come with every trip sheet.
- **H2 Hear it for yourself:** `Push the button at aichauffeur.ai/try. It's not a phone call — it talks to you right there in your browser. Book a trip like you're the customer. When you're done, the trip, the recording, and the words are on the page.` / `Or call it like you're booking a trip: (414) 775-0019.` + green PUSH TO BOOK (`data-event="wid_push_to_book"`).
- **Four FAQs** (answers drafted in § 5.1): Does it quote the price on the call? · What if the trip isn't on my rate sheet? · Where does the trip go after the call? · Is the call recorded?
- **Last-updated stamp.**

### 4.4 `/what-it-can-do/`

- **Title:** `What It Can Do For Your Company | AI Chauffeur`
- **Description:** `Live transfer, repeat callers by name, account and card lookup, your CRM, Spanish, a second brand. AI Chauffeur is set up for how your company runs its phones.`
- **H1:** `What it can do for your company`
- **Answer first (41 words):** `Every company runs its phones a little differently. These are set up for your company when we build your desk. Live transfer, repeat callers by name, account and card lookup, your CRM, Spanish, a second brand — your words, your rules, your policies.`
- **Table (the one table):** Option · What it does — the bullets below (option labels drafted in § 5.2).
- **Bullets:**
  - Live transfer to your owner, your on-call person, or your dispatcher — any time, or only when the caller needs a person.
  - Take every call your office can't get to — all day as your backup line, or as the whole reservation line so your people only dispatch.
  - Recognize repeat callers by their phone number and greet them by name.
  - Look up the customer's account and the card on file.
  - Connect to the software you already run your trips in, so the trip lands where your dispatcher works.
  - Send the trip straight to the driver on duty.
  - Take affiliate calls and book the farm-in like any other trip.
  - Take calls in Spanish.
  - A second number for a second company or brand — same desk, different name.
  - Say things the way your dispatcher says them. Your words, your rules, your policies.
  - Every call recorded, every trip sheet kept, so you can hear any call back.
- **Close:** `Push the button. Hear it for yourself.` → green PUSH TO BOOK → `/try/` (`data-event="wicd_push_to_book"`) · `(414) 775-0019`
- **Four FAQs** (answers drafted in § 5.1): Can it transfer to a person? · Does it know repeat customers? · Can it work with the software I already use? · Does it speak Spanish?
- **Last-updated stamp.**

### 4.5 AVA → AI Chauffeur (every page)

| Now | Becomes |
|---|---|
| "AVA answers. Hear it yourself." | `AI Chauffeur answers. Hear it yourself.` |
| "AVA CALLS YOU" (markup is sentence case "AVA calls you", uppercased by CSS) | `AI Chauffeur calls you` (renders `AI CHAUFFEUR CALLS YOU`) |
| "Drop your cell and AVA calls you instead." | `Drop your cell and AI Chauffeur calls you.` |
| Console speaker `AVA:` | `AI CHAUFFEUR:` |
| Console opener | `AI Chauffeur, where are you headed today?` |
| "I have it all down for your dispatcher." | `Booked. Your trip sheet is on its way.` |
| "18.4 mi · $65–$85 est." | `$145` (one clean number, sedan transfer minimum) |
| Console status | `CALL ANSWERED → QUOTED → BOOKED → IN YOUR CRM` |
| "Nobody sits on hold. Nobody hangs up." and any "sounds human" line | deleted |
| Titles and meta | same swap |

### 4.6 Money buttons (every page)

- **Green fill:** hero PUSH TO BOOK · section-close PUSH TO BOOK · both new-page PUSH TO BOOKs · `/book/` submit.
- **Green outline:** header phone chip `Call (414) 775-0019` · header `Book the setup call`.
- Everything else unchanged.

## 5 · Drafted from your lines — approve at GO

Nothing below is new wording of a claim; each line is cut from § 4. Approve, edit, or strike at GO.

### 5.1 FAQ answers (two sentences each)

**`/what-it-does/`**

| Question | Draft answer |
|---|---|
| Does it quote the price on the call? | Yes. It knows your rate sheet and gives the customer one clean number, service fee included. |
| What if the trip isn't on my rate sheet? | It prices the trip by the miles and the drive time. Anything missing is flagged for review. (follows § 9 D6) |
| Where does the trip go after the call? | It puts the trip in your CRM, emails you the trip sheet, and sends a text alert to your phone. The customer gets their trip by text and email. |
| Is the call recorded? | Yes, every call is recorded. The recording and every word of the call come with the trip sheet. |

**`/what-it-can-do/`**

| Question | Draft answer |
|---|---|
| Can it transfer to a person? | Yes, once it is set up for your company. It hands the call to your owner, your on-call person, or your dispatcher — any time, or only when the caller needs a person. |
| Does it know repeat customers? | It can. It is set up to recognize repeat callers by their phone number and greet them by name. |
| Can it work with the software I already use? | It can be connected to the software you already run your trips in. Then the trip lands where your dispatcher works. |
| Does it speak Spanish? | It can take calls in Spanish. It is turned on when we build your desk. |

### 5.2 `/what-it-can-do/` table — Option labels

| Option | What it does (verbatim from § 4.4) |
|---|---|
| Live transfer | Live transfer to your owner, your on-call person, or your dispatcher — any time, or only when the caller needs a person. |
| Backup or full line | Take every call your office can't get to — all day as your backup line, or as the whole reservation line so your people only dispatch. |
| Repeat callers | Recognize repeat callers by their phone number and greet them by name. |
| Account and card lookup | Look up the customer's account and the card on file. |
| Your software | Connect to the software you already run your trips in, so the trip lands where your dispatcher works. |
| Driver on duty | Send the trip straight to the driver on duty. |
| Affiliate calls | Take affiliate calls and book the farm-in like any other trip. |
| Spanish | Take calls in Spanish. |
| Second brand | A second number for a second company or brand — same desk, different name. |
| Your words | Say things the way your dispatcher says them. Your words, your rules, your policies. |
| Call recordings | Every call recorded, every trip sheet kept, so you can hear any call back. |

### 5.3 Homepage lines outside the hero and the new section

Needed so the homepage stops contradicting itself (§ 9 D15) and so ruling 5 lands. `[text]` marks a link that already exists.

| Where (`chauffeur/index.html`) | Now | Draft |
|---|---|---|
| `#pain` :583 | "Voicemail, after-hours, the same five questions every call, three calls hitting at once on a busy night. AVA picks up all of them … and the reason the after-hours window costs the most." | Voicemail, the same five questions every call, three calls hitting at once. AI Chauffeur picks up all of them and takes the whole trip down, so your dispatcher gets a finished trip instead of building one from scratch. That is the job a [limo answering service built for dispatch] is supposed to do, and it matters most for [the calls your office can't get to]. |
| `#crush` :695 link label | "Work out what a missed night costs you →" | Work out what the calls your office can't get to cost you → (target unchanged: `/after-hours-limo-dispatch/#missed-night`) |
| `#crush` :643 | "…built to absorb those windows; AVA never lets a call sit on hold." | …built to absorb those windows and never lets a call sit on hold. |
| `#integrations` :525 | "AVA takes the call; your dispatcher makes the booking." | AI Chauffeur takes the call and books the trip; your dispatcher runs it. |
| `#how` :556-558 | "03 · Estimate" · "Quote range calculated from your rate sheet, service area, surge windows, and vehicle class — not a generic guess." | 03 · Quote · One all-in quote from your rate sheet, service area, time of day, and vehicle class. Not a generic guess. |
| `#demo` :429 | "AVA captures the trip, estimates a quote range … call the demo line and give AVA a real trip." | A caller speaks naturally. AI Chauffeur takes the trip, quotes your rate sheet, handles mid-call changes, and books it. The fastest way to judge that is to [push the button and book a trip] yourself. (link → `/try/`) |
| `#demo` sample console :491 | "Quote range" · "$285 – $325" · "90 mi · est. 1h 50m drive · tolls included" | Quote · $292.50 · 90 mi · 1h 50m drive · service fee included (engine math for that sample: Executive SUV, max of $185 floor, 90 mi × $3.25, 1h 50m × $145 — `wf-rate.02-compute-quote.js:26`, `:488-490`) — § 9 D10 |
| Meta description :8 | "AVA answers your dispatch line 24/7, takes the whole trip, and hands your dispatcher a clean ticket. Built by operators. Call (414) 775-0019." | AI Chauffeur answers twenty calls at once, takes the whole trip, quotes your rates, books it, and puts it in your CRM. Call (414) 775-0019. |
| og / twitter description :31, :40 | same, without the phone | the draft above, without "Call (414) 775-0019." |

**Homepage FAQ** (visible answers; the JSON-LD is regenerated from them by `tools/aic-run10-faq-mirror.py`):

| Question | Draft answer |
|---|---|
| Is this just basic call coverage? | No. AI Chauffeur answers your phones and books the trips. It takes the whole trip, quotes your rates, and puts it in your CRM. |
| Does it replace my dispatcher? | No. It takes the phones so your dispatcher is not buried by missed calls and repeated questions. Your people run the cars, and your dispatcher still runs the operation. Which half of the job gets automated, and which half never will, is set out on [limo dispatch automation]. |
| What still requires dispatcher approval? | Assigning the car and the driver. AI Chauffeur books the trip on the call and puts it in your CRM, and anything missing is flagged for review. It does not take payment. |
| How does AI Chauffeur handle multiple calls at once? | It answers every line at the same time, twenty calls at once, so no caller waits on hold. Each call gets its own trip: pickup, drop-off, time, vehicle, passengers, bags, and flight. Every one lands as its own trip sheet. |
| Can it quote rides? | Yes. It quotes your rates on the call and gives one clean number, service fee included. The demo runs a sample rate card; your desk runs yours. |
| Does it work with my CRM or dispatch software? | Yes. It puts the trip in your CRM, and it can be connected to the dispatch software you already run. Where each platform stands is on the [integrations page]. |
| Can I just hear it first? | Yes. [Push the button] and talk to it in your browser, or call [(414) 775-0019] and AI Chauffeur picks up. It is the same agent your callers would reach, with no form and no salesperson. |

"How fast can it go live?" and "What about pricing?" stay as they are.

**Homepage JSON-LD** (`tools/truth_audit.py` needs each string verbatim in the visible page): Service `name` → the new H1 · Service and SoftwareApplication `description` → the new hero sub · SoftwareApplication `name` "AVA" → `AI Chauffeur` · `featureList` → the six bullet leads: `Twenty calls at once.` · `The whole trip, like a dispatcher.` · `Your rates, quoted live.` · `Booked. In your CRM.` · `Trip sheet on your phone.` · `Every call recorded.`

### 5.4 Strings on every page

| Where | Now | Draft |
|---|---|---|
| CTA stack sub-line (13 pages) | "AVA answers. Hear it yourself." | AI Chauffeur answers. Hear it yourself. (your wording) |
| Callback form title / sub / submit (13 forms) | "AVA calls you" · "Busy right now? Drop your cell and AVA calls you instead." · "Have AVA call me" | AI Chauffeur calls you · Drop your cell and AI Chauffeur calls you. · Have AI Chauffeur call me |
| `chauffeur/assets/aic.js` notes :233, :240, :415, :423, :472, :476, :479 | "AVA" in 7 visible messages | same messages with "AI Chauffeur" |
| Sign-off `/rates/` :598, `/reserve/` :514 | "AVA Team" | AI Chauffeur Team |
| `/demo/` body links → `/try/` (limo-answering :291, works :395, airport :359) | "Call the line and hear one →" | Push the button and hear one → |
| Related cards → `/try/` (book :334, airport :449, how-setup :396, limo-answering :421, works :535) | "Call the line and hear it captured" | Push the button and hear it book a trip |
| Related cards (after-hours :461, milwaukee :427, madison :494) and homepage :442 | "Run a sample intake / call yourself" · "Run it yourself" | unchanged; href → `/try/` |
| Nav, drawer, footer "Live demo" (17 pages) | → `/demo/` | label unchanged; href → `/try/` |
| Second in-body link, `/limo-answering-service/` | — | Everything it does on a call is on [what AI Chauffeur does]. (→ `/what-it-does/`) |
| Second in-body link, `/how-setup-works/` | — | What can be set up for your company is on [what it can do for your company]. (→ `/what-it-can-do/`) |
| New section bullet 2, homepage | — | "flight number" links to `/airport-transfer-booking/` (keeps the in-body link that `#features` carried; no words added) |
| `/what-it-does/` description | 163 characters | AI Chauffeur answers the phones for limo and black car companies, quotes your rates, books the trip, and puts it in your CRM. Twenty calls at once. (147) |
| `/what-it-can-do/` description | 159 characters | Live transfer, repeat callers by name, account lookup, your CRM, Spanish, a second brand. AI Chauffeur is set up for how your company runs its phones. (150) |

### 5.5 `chauffeur/llms.txt`

- **Summary (:9-21):** AI Chauffeur answers the phones for limousine and black car companies and books the trips. It takes twenty calls at once, takes the whole trip, quotes the operator's rates, books it, and puts it in the operator's CRM. On each call it captures pickup, drop-off, date and time, vehicle, passengers and bags, child seats, extra stops, and the flight number for airport work, and gives the caller one all-in number, service fee included. The operator gets the trip sheet by email and a text alert. Every call is recorded.
- **"The two ways in" → "The ways in":** 1. Push the button — https://aichauffeur.ai/try/ — and talk to it in the browser; the demo uses a sample rate card and puts no car on the road. 2. Call (414) 775-0019; the same agent answers. 3. Book the setup call — https://aichauffeur.ai/book/ — twenty minutes.
- **Pages:** drop the Live demo entry; add `What it does` and `What it can do for your company`; the phone line bullet reads "AI Chauffeur answers it."
- **"Not true of AI Chauffeur":** It does not take payment or charge a card. · It does not replace a dispatcher; your dispatcher assigns the car and the driver. · It does not write into an operator's dispatch software until that connection has been set up and tested for the account; trips go into the CRM from day one. · A reservation hold on /reserve/ is a request, not a booking; a dispatcher calls back to set the trip. · Not affiliated with or endorsed by Google or by any dispatch platform named on the site, including Limo Anywhere, FASTTRAK and Santa Cruz. · Demo rates are a sample rate card; a live desk runs the operator's own rates.

## 6 · File map

**Create:** `chauffeur/what-it-does/index.html` · `chauffeur/what-it-can-do/index.html` · `reports/2026-09-13-aic-words-v2.md` (readback mirror, at DONE)

**Delete:** `chauffeur/demo/index.html`

**Modify — site**
- `chauffeur/index.html` — hero (eyebrow, H1, sub, position line, PUSH TO BOOK, the two lines; form, `data-rail-after` and console kept) · console copy and its timeline script (≈ :995-1096) · `#features` (:587-627) → the new section · § 5.3 lines · FAQ · JSON-LD · meta · chrome · AVA in comments.
- `chauffeur/try/index.html` — remove the v7 button rules (:224-274) from its `<style>`; `#try-btn` gets `btn-go` beside `try-btn`. The module script is not edited.
- The other 16 chrome pages (`limo-answering-service`, `after-hours-limo-dispatch`, `milwaukee-limo-answering-service`, `madison-limo-answering-service`, `airport-transfer-booking`, `how-setup-works`, `works-with-your-software`, `limo-dispatch-automation`, `integrations`, `integrations/limo-anywhere`, `integrations/fasttrak`, `book`, `rates`, `reserve`, `privacy`, `terms`) — Product dropdown, drawer Product group and footer column 1 gain What it does + What it can do, "Live demo" → `/try/` · § 5.4 strings · every AVA (visible, meta, JSON-LD, comments) · `rates` :585 phone format.
- `chauffeur/assets/aic.css` — `.btn-go` after the primary block that ends at :2233 · `.nav-cta` and `.nav-book` green outline at all widths, drawer included · position line, proof row and new-section rules · AVA in a comment (:666).
- `chauffeur/assets/aic.js` — 7 message strings and 4 comments. `ENDPOINT …/ava-call` stays (n8n path).
- `chauffeur/assets/circulant.css` (3 comments) · `chauffeur/site.webmanifest` (1 string).
- `chauffeur/vercel.json` — `/demo` and `/demo/` → `/try/`, `"permanent": true` (308).
- `chauffeur/sitemap.xml` — remove `/demo/`, add the two pages: 19 `<loc>`.
- `chauffeur/llms.txt` — § 5.5.

**Modify — law, tools, ledger:** `chauffeur/DESIGN-SYSTEM.md` (Task 2) · `CLAUDE.md` (one pointer line under § 8) · gate tools (§ 8.2) · `tools/stamp.py:263` (drop the `/demo/` entry; nothing is run) · `hq/board.json` (new item `aic-words-v2`, `aic-try` label, LOG entry).

**Never touched:** `chauffeur/assets/brand/*` · root favicon files · `chauffeur/fonts/` · the `/book/` iframe loader · form field names and endpoints (`id="ava-callback"`, `/webhook/ava-call`, `/webhook/ava-intake`) · the `/try/` module script · n8n · Retell · GHL.

## 7 · Tasks

**Task 0 · Customer-text proof** — method set by § 9 D7. Nothing runs until GO answers it.

**Task 1 · Baseline (no edits)**
- [ ] `git worktree add ../aic-base 4a12037`
- [ ] `node tools/aic-run13-parity.mjs snap base --site ../aic-base/chauffeur`
- [ ] `/try/` screenshots with animations frozen, 5 states × 390 and 430 at DPR 2, using the stubs in `tools/aic-try/render.mjs` → `audits/aic-words-v2/try-base-*`
- [ ] Record today's gate reds. Expected, and not caused by this run: run10 FAQ mirror (3 pages) · `faq-mirror --check` formatting (13 pages) · run12 check 9 · run12 check 11.
- [ ] Production Lighthouse on `/`, median of 3 (perf, CLS) — the bar the glow must not lower.

**Task 2 · Law** — `chauffeur/DESIGN-SYSTEM.md`: § 2 roles (`--success-green` also marks money buttons) · STATE LAW :139 · § 3 primary control :260-304 (adds the `.btn-go` recipe: green ramp face, 2px mint edge, `#070B14` 700 caps label, rim glow in light only, `data-state` faces, reduced motion) · CTA DE-DUPLICATION :326-341 (chip = green outline at every width) · CLAIM LADDER :502-530 (CRM superseded by ruling 3; the dispatch-software write-back ladder stays) · GLOW LAW :551 (money-button exception; runs alongside the scene pulses; the button never scales) · NO-NAME LAW :596-597 (AVA line → AI Chauffeur) · CTA CANON :623 and the Google section fixed copy :630-637 (name swap) · NAV + FOOTER MAP :652-701 · page counts in § 1 and § 11. `CLAUDE.md` § 8: one pointer line.

**Task 3 · Gates** — every repair in § 8.2 lands with a negative control that proves it can fail. Run the gates against the baseline worktree: only the four expected reds may show.

**Task 4 · `.btn-go`**
- [ ] Lift `try/index.html:224-274` into `aic.css` after :2233. `.try .try-btn` → `.btn-go`; state rules stay `.try[data-state=…] .btn-go`; the glow runs on `.btn-go` outside `.try` and on `.try[data-state="ready"] .btn-go`; hover pause on `.btn-go:hover` and `.try-push:hover .btn-go`; reduced motion generalised to `.btn-go`; `@keyframes try-glow` keeps its name; host `background:none` in every state (the sliver fix).
- [ ] `/try/`: remove the lifted rules, add `btn-go`.
- [ ] `node tools/aic-try/render.mjs` green locally; screenshot diff against Task 1 = 0 px in all 5 states and reduced motion.
- [ ] **Commit 1** (law + gates + `.btn-go` + `/try/`), explicit paths only.

**Task 5 · Homepage hero and console** (§ 4.1, § 4.5, § 9 D2 · D21 · D22).

**Task 6 · Homepage body** — new section replaces `#features` (§ 4.2) · § 5.3 lines · FAQ · JSON-LD · meta · then `python tools/aic-run10-faq-mirror.py` (write mode, last).

**Task 7 · The two pages** — anatomy copied from `chauffeur/limo-dispatch-automation/index.html`: `section.phead` + crumbs + `h1.page-h` + `.answer` + `.page-updated` · sections · one table (`/what-it-does/`: 3 columns in `.tbl-wrap` with `tabindex="0"`, `role="region"`, `aria-label` and `.tbl-hint`; `/what-it-can-do/`: `.tbl--pair`) · `#faq` with bare `<details><summary><div>` · CTA · related links · JSON-LD Organization, WebSite, Service, empty FAQPage (the mirror fills it), BreadcrumbList · self canonical · `<!-- source: Retell account concurrency, 20 base, ruling 2026-09-13 -->` beside "Twenty calls at once".

**Task 8 · Chrome and strings on the 17 pages** (§ 5.4) · AVA sweep · phone format · chip and book outline (CSS).

**Task 9 · `/demo/` out** — delete the page · `vercel.json` redirect · sitemap · `llms.txt` (§ 5.5) · `tools/stamp.py:263`.

**Task 10 · Sweeps** — greps in § 8.1 · `python tools/stamp_chauffeur.py` · `git add` the new pages, then `python tools/truth_audit.py`.

**Task 11 · Local proof** — all gates green · EYES shots at 390×844 and 1440 (homepage, both new pages, `/try/`) · fold check (§ 9 D22) · **Commit 2** (site) · push.

**Task 12 · Production** — gates against `https://aichauffeur.ai` · Lighthouse median of 3 · `/demo/` → 308 → `/try/` · sitemap serves 19 · `node tools/aic-try/render.mjs --prod` · `hq/board.json` · report · DONE table.

## 8 · Checks

### 8.1 Named checks

**Greps** (shipped files under `chauffeur/`; `DESIGN-SYSTEM.md` is not deployed):
- `\bAVA\b` (case-sensitive) = 0 in html, txt, css, js, webmanifest.
- `nobody` (case-insensitive) = 0 on `/`, `/what-it-does/`, `/what-it-can-do/`, `/try/` (scope: § 9 D17).
- `#00D4FF` = 0 outside `chauffeur/assets/brand/` and the root favicon files · `shane` / `founder` = 0 — `python tools/aic-run10-greps.py`.
- `414-240-8930`, `350-220-5305`, `(350) 220-5305` = 0 · `href="/demo` = 0.
- Titles ≤ 60 and descriptions ≤ 155 on `/`, `/what-it-does/`, `/what-it-can-do/`.
- `data-event` present: `hero_push_to_book`, `nav_call`, `nav_book`, `wid_push_to_book`, `wicd_push_to_book`.

**Tools:**
- `python tools/aic-run10-faq-mirror.py --check` (after G11) · `python tools/stamp_chauffeur.py --check` · `python tools/truth_audit.py` · `python tools/sitemap_lastmod.py`
- `node tools/aic-run9-gate.mjs` · `node tools/aic-run9-styleparity.mjs` · `node tools/aic-run10-gate.mjs` · `node tools/aic-run11-gate.mjs` · `node tools/aic-run14-gate.mjs` · `node tools/aic-run12-gate.mjs https://aichauffeur.ai`
- `node tools/aic-try/render.mjs` (local, then `--prod`)
- `node tools/aic-run13-parity.mjs snap after` then `diff base after` — only intended nodes may move.
- `node tools/aic-run12-lighthouse.js https://aichauffeur.ai` — median of 3; a11y and SEO 100; CLS < 0.0005; homepage perf not below the Task 1 bar.
- From Git Bash, `export MSYS_NO_PATHCONV=1` before any path argument.
- Production: `curl -sI https://aichauffeur.ai/demo/` → 308 to `/try/` · both new pages 200 · sitemap lists both.
- EYES: homepage at 390×844 and 1440, both new pages at 390 and 1440, `/try/` at 390 — opened and looked at.

### 8.2 Gate repairs (from the read-only gate audit)

| # | Where | Problem | Repair |
|---|---|---|---|
| G1 | `aic-run11-gate.mjs:542-551` | The drop-shadow regex stops at the colour's own `)`, so "elevation is neutral" can never fail. | Match every layer (`/drop-shadow\((?:[^()]\|\([^)]*\))*\)/g`); allow `46,230,168` and `170,255,225` only on `.btn-go`; add a planted coloured drop-shadow as a negative control. |
| G2 | `aic-run10-gate.mjs:89` · `aic-run11-gate.mjs:41` | Filled-control probes know only blue; the green face is a `::before` background-image. | Add a separate green detector for content controls; keep the blue list for chrome, tel and the blue primary recipe; negative controls at `aic-run11-gate.mjs:296`, `:314`, `:324` plant green where they test content. |
| G3 | `aic-run11-gate.mjs:529-540` | The primary recipe expects a blue `::before`. | `.btn-go` branch: green ramp on `::before`, literal label "PUSH TO BOOK". |
| G4 | `aic-run9-gate.mjs:61-72` · `aic-run14-gate.mjs:59-68` | Contrast grounds read only `background-color`, so PUSH TO BOOK measures ≈1:1. | Read `::before` gradient stops as the ground (port `tools/aic-try/lib.mjs:129-154`). |
| G5 | `aic-run9-gate.mjs:361-380` | Any animation still running on `/` under reduced motion fails. | Met by the generalised `.btn-go` reduce rule (Task 4); verify only. |
| G6 | `aic-run11-gate.mjs:726`, `:779-784`, `:820-821` · `aic-run11-shots.mjs:94`, `:108-109` | They tap and hover the hero `.tel-btn`, which is gone (crash on `null`). | Retarget to `#setup .btn-primary` (`index.html:744`), scrolled into view. |
| G7 | `aic-run10-gate.mjs:163-173`, `:190` | The filled-CTA negative control needed the hero tel button in the fold. | BREAK injects its own two `a[href^="tel:"]` into the first section. |
| G8 | page lists: `aic-run9-gate.mjs:25-48` · `aic-run9-styleparity.mjs:85` · `aic-run10-gate.mjs:23-38` · `aic-run11-gate.mjs:23-38`, `:337-338` · `aic-run12-gate.mjs:41-58`, `:208-213` · `aic-run12-lighthouse.js:32-38` · `aic-run13-parity.mjs:88-105` · `aic-run14-gate.mjs:30-35` · `aic-run9-lighthouse.js:28-36` · `aic-run9-livediff.mjs:22-39` · `aic-run12-shots.mjs:35-40` | `/demo/` is hard-coded (local 404, production 308); two tools crash. | Drop `/demo/`; add `/what-it-does/` and `/what-it-can-do/` (and `/rates/`, `/reserve/` where a list claims every page); add the new section id to the homepage anchors; `/llms.txt` into the live-diff assets. |
| G9 | `aic-run12-gate.mjs:462-470` | Sitemap check is already red (`/rates/`, `/reserve/` missing from its list). | Own 19-URL list; assert `/try/` absent; derive the "16" labels (`:380`, `:423`, `:458`, `:467`) from list lengths. |
| G10 | `aic-run12-gate.mjs:41-46` | New pages are not in NEW. | Add both, so checks 1, 2, 5, 6, 7, 12 and 13 cover them. |
| G11 | `aic-run10-faq-mirror.py:99-110` | `--check` compares text, so every pretty-printed block fails. | Compare parsed JSON values; run write mode last. |
| G12 | `aic-run10-gate.mjs:261` | Ledger label check. | Unchanged — The Crush ledger keeps "Ready for dispatch" (§ 9 D21). |
| G13 | `tools/aic-try/render.mjs:225`, `:599` | Needs `@keyframes try-glow` and class `try-btn`. | Keep both beside `btn-go`. |
| G14 | `aic-run10-greps.py:100` | Negative control needs a "414-775-0019" hit. | JSON-LD `telephone` stays `+1-414-775-0019`. |
| G15 | `aic-run13-parity.mjs:201` | Never reads pseudo-elements; no `/try/`; no state control — it cannot prove the `/try/` lift. | `/try/` proof = `render.mjs` + the frozen screenshot diff (Task 4); parity covers the other pages. |
| G16 | — | No tool checks the `/demo/` redirect, `DESIGN-SYSTEM.md`, `data-event` names, or "one active pulse at a time". | Checked by hand at Task 12. |

## 9 · Decisions at GO

Reply **GO** to take every default, or override by number (e.g. "GO, D3=b, D7=b").

| # | Decision | Default | Other options |
|---|---|---|---|
| D1 | Quoting GOTCHA wording (ruling 2 stands) | "The demo quotes only when a call reaches the price: 1 quote in 7 days (Sep 6, by phone); none of the 12 browser demos got that far." | (b) "the live demo does not yet quote a price" — contradicted by § 3 C15 |
| D2 | Console "$145" | `$78` — what the engine returns for that trip | (b) keep $145 and move the console trip onto the miles path; (c) keep $145 unsourced |
| D3 | "If there's no card on file, it sends a secure link to pay." | Strike it from `/what-it-does/` | (b) move it to `/what-it-can-do/` as build-to-order; (c) ship + GOTCHA |
| D4 | "more people or bags than the car holds" | "If there are more people than the car holds, it moves the trip up to a bigger car." Engine fix (`:322`) goes to the automation lane. | (b) ship + GOTCHA |
| D5 | "every stop" in the emailed trip sheet | Drop "every stop," | (b) ship + GOTCHA |
| D6 | "flagged for review before dispatch confirms it" | "Anything missing is flagged for review." | (b) ship as written |
| D7 | Customer-text proof (Task 0) | You place one live call at aichauffeur.ai/try on your phone. Book a full airport trip, give the cell already on your demo-caller contact (**never** the owner-alert cell), say "yes, you can text me", and give the email on that same contact. I read the n8n execution: quote, SMS message id, email, CRM write. A one-time § 9 exception that you approve. | (b) No call: cite the Sep 3–6 internal sends; copy stands; GOTCHA says no outside caller has received one yet. Note: your fallback line ("text to you, email to the customer") has the same record — customer email is no better proven than customer text. |
| D8 | `/what-it-can-do/` options not built anywhere (live transfer, Spanish, card lookup, driver on duty, affiliate farm-in; repeat callers built but not wired) | Ship as build-to-order, as the page frames it — you confirm each will be built on order | (b) strike the ones you won't build |
| D9 | Money buttons in the law | Exactly your list (hero and close PUSH TO BOOK, new-page PUSH TO BOOKs, header chip and header book as outlines). Every other booking or call control stays as it is. | (b) every agent or booking CTA goes green (≈40 controls on 19 pages, rail and gates) |
| D10 | `#demo` sample console range "$285 – $325" | One number, `$292.50`, from the engine's math for that sample (§ 5.3) | (b) leave the range |
| D11 | `/book/` booking button green | You set it in the GHL calendar widget settings — our CSS cannot reach the iframe. Face `#2EE6A8`, label `#070B14`. | (b) skip |
| D12 | "No prices" | No AI Chauffeur pricing on those pages; sample trip fares allowed (console, `#demo`) | (b) no dollar figures at all |
| D13 | `/try/` chrome | Stays chromeless. "All 19" = the 17 existing chrome pages once `/demo/` goes + the 2 new ones. | (b) add nav and footer to `/try/` (breaks its one-screen design and `render.mjs`) |
| D14 | Gate repairs G1–G16 | All in this run, each with a negative control | (b) ship pages, leave gates red |
| D15 | 147 contradicting lines on 16 pages ("a person confirms every trip", "illustrative quote ranges", "does not take payment", "not a commitment") | This run fixes the homepage (§ 5.3) and `llms.txt` (§ 5.5). The other 15 pages get the name swap only; a follow-up sweep run handles their wording. | (b) sweep all 16 now — drafts for approval first; (c) homepage copy only as briefed — the homepage then contradicts itself |
| D16 | AVA on legal pages (`/privacy/` 11, `/terms/` 7) | Name swap only; nothing the terms say changes | (b) leave legal pages for counsel |
| D17 | `nobody` = 0 | Homepage, both new pages, `/try/`. The ~30 lines on 11 other pages go to the sweep run. | (b) all pages now |
| D18 | Analytics | Ship the event names. Nothing reads them today (no GA4, Meta or Vercel script); tracking is its own run. | (b) add tracking in this run |
| D19 | Glow defects found in passing (Ringing chip pulses offscreen; Crush clock dot pulses forever and shows with JS off; console empty with JS off) | Report only (§ 10) | (b) fix the CSS in this run |
| D20 | OG card `chauffeur/assets/og-card.png` is cyan | Leave; a new card is a brand-kit file for a later run | (b) hold the run for a new card |
| D21 | Console statuses | CALL ANSWERED → QUOTED → BOOKED → IN YOUR CRM on the header line, the chip sequence and the two badges; "Booked." is spoken after QUOTED. Chip colours: Ringing amber → Call answered Signal blue → Quoted, Booked, In your CRM green. The Crush ledger is unchanged. | (b) header line only |
| D22 | Hero fold on a phone | If PUSH TO BOOK lands below 844px: first hide sub sentences 2–5 under 768px (the site's existing `.hero-sub-more` pattern), then move the position line under the button on phones | (b) shrink the H1 on phones instead |
| D23 | `CLAUDE.md` carries an uncommitted 49-line RETELL API RULES block (Aug 17) | Commit it on its own first, then add the pointer line | (b) stage only the pointer line |
| D24 | Board item for the new pages | Create `aic-words-v2` (planned → live) with a LOG entry; update the `aic-try` label | — |

## 10 · GOTCHAS carried into DONE (not fixed by this run)

- **Quoting record** — worded per D1.
- **The browser demo** writes no CRM record and can send no customer text (no caller ID). Its receipt says "The same details were texted to the number you gave" whenever a mobile is captured, without checking consent or the send (`chauffeur/try/index.html:335`; the module script is out of scope).
- **The agent still calls itself AVA** — prompt identity "You are AVA" and the callback opener "this is AVA calling back". The site's "AI Chauffeur calls you" form starts a call that says AVA. Voice lane (PROMPT AUTHORITY LOCK).
- **Brand crossing** — owner and customer texts on AI Chauffeur calls go out from the AVA parent's published text line; `/reserve/` :510-512 sends visitors to book with AI Voice Agency.
- **§ 9 breaks on the demo rail** (`TkETvvnABhUPd7ME`) — "Upsert Caller in GHL" has no our-numbers check; "Ensure Owner Contact" rewrites the owner-alert contact on every run; the owner email, sender and a fallback contact id are hard-coded in node bodies; no repo export of the rail exists.
- **Security** — a shared secret sits in plain text in the v27 flow's tool settings; § 9 says n8n Variables only. Rotate and move.
- **Rate engine and docs** — `wf-rate.02-compute-quote.js:322` reads `vc.cap` (bags never upsize) · `schema/aic-reservation-v1.json` still says CAPTURE_ONLY · `docs/AIC-VOICE-STATE.md` still names V13 · `automation/rate-engine/README.md` zone count is stale (59 live rows, duplicate pairs).
- **`/rates/`** shows 15 classes against 18 priced (left alone by ruling).
- **0019 has no inbound webhook**, so a known caller gets the cold greeting.
- **`/privacy/` :247** lists analytics tools; none are installed.
- **Existing glow defects** (D19) and the **cyan OG card** (D20).

## 11 · GO — Sep 13 2026

Every § 9 default holds except these overrides.

- **D7:** the customer-text proof moves to the phone path and is done by Shane (a call to (414) 775-0019 from his demo-caller cell). Until he reports the text landed, the copy reads "The customer gets their trip sheet by text" — no "and email" anywhere. No `/try/` call with any of our numbers. "The `/try/` flow never asks for the customer's mobile" is a Conductor item, not a code edit.
- **D2:** `$78` on the console. Every other sample fare on the homepage goes (the `$285 – $325` card is removed). One fare, one trip, engine-true.
- **D3:** the pay-link line is struck everywhere (homepage, `/what-it-does/`, FAQ). Nothing about cards or payment stays except `/reserve/`'s "No card, no charge."
- **D8:** live transfer, Spanish, card/account lookup, driver on duty and affiliate farm-in are real build-to-order offers, quoted per build, kept on `/what-it-can-do/` under "These are set up for your company when we build your desk." Repeat callers by name: built, not wired to the line (GOTCHA).
- **Bags:** "more people than the car holds" only, until `wf-rate.02-compute-quote.js:322` is fixed (GOTCHA, its own follow-up).
- **Trip sheet:** "every stop" → "every note for the chauffeur".
- **Review:** "before dispatch confirms it" → "flagged for review, so nothing slips."
- **Homepage FAQ:** rewritten from the new copy — no "illustrative quote ranges", no "does not take payment", no named CRM or dispatch product.
- **Analytics:** Vercel Web Analytics on every page this run touches (the insights script plus the `window.va` queue shim); `data-event` clicks call `va('event', {name})`. Shane enables Web Analytics on the project in the Vercel dashboard (NEXT). The privacy page's analytics sentence is corrected to what runs.
- **D11:** Shane sets the `/book/` widget button green in GHL (NEXT).
- **Log, don't touch:** the agent prompt and callback opener still say AVA (Conductor) · the `/try/` flow never asks for the customer's mobile (Conductor) · the bags bug (its own fix) · the shared secret stored in plain text in the agent settings (its own fix under Doppler, this week) · texts sent from the AVA parent's line (noted).
- **Read at GO:** "no named CRM or dispatch product — ever, on any page" is applied to every page this run writes (homepage body, FAQ and JSON-LD; both new pages; `llms.txt` prose) and written into `DESIGN-SYSTEM.md` as standing law. The two integration pages, the nav/drawer/footer links that name them, and `/works-with-your-software/` are recorded exceptions queued for the D15 sweep, because removing them takes two indexed pages and their inbound links off the site in a words run.
- **Verify:** zero `\bAVA\b` in visible text on aichauffeur.ai (code hooks excepted and listed) · zero `nobody` on rewritten pages · zero `#00D4FF` outside logo files · 390×844 and desktop shots of the homepage, `/what-it-does/`, `/what-it-can-do/`, `/try/` · board, report, DONE.
