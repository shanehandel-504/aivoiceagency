# /reports

Run readbacks — one file per mission, named `YYYY-MM-DD-<mission>.md`.

Each file mirrors the fenced `===== SHANE READBACK — COPY ALL =====` block printed at the
end of that run (plain-English summary · DONE table · IDs / rollback · what's next · gotchas).

This folder is internal: it is listed in `.vercelignore` and is **not** deployed to the public site.

- 2026-07-09 — THE FINAL CUT (homepage freeze pass) → `2026-07-09-final-cut.md`
