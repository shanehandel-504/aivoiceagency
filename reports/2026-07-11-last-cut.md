# THE LAST CUT — final site pass — 2026-07-11

Repo `shanehandel-504/aivoiceagency` → Vercel (auto-deploy from `main`). Production: https://aivoiceagency.ai/

This run un-froze the homepage (per founder mandate "THE LAST CUT — grab everything, ship impressive")
and re-froze it at this baseline. All work is LIVE and verified on production.

---

## Plain-English summary

The homepage got its last polish before launch:

1. **Every demo call was re-recorded.** AVA's lines were re-rendered in her real voice at the tuned
   "Closer" settings, and every caller was re-cast with a *different, natural-sounding* premium voice —
   so the person on the other end of each call sounds like a real customer, not a robot. New audio lives
   in a separate folder; the old audio is untouched, so we can undo it in one step if you want a different voice.
2. **The "Call me" box in the phone widget** used to be dark-on-dark and hard to see. It's now a clean
   **white card** with a visible number field and the consent checkbox. The tab itself is now the
   **"📞 Call me live"** beacon with a soft green pulse — and we made sure only ONE green pulse blinks on
   screen at a time so the page never looks like a Christmas tree.
3. **The plain boxes got depth.** FAQ cards, the call transcripts, and the info boxes now carry the same
   glowing left-edge you love from the scope note — cyan by default, **gold on the money boxes** (the cost
   answer and the "revenue left on the table" calculator).
4. **Micro-polish:** footer links now glow on hover. (The audio-reactive header dot and the desktop
   pricing-card lift + tilt were already live from HOMEPAGE CANDY.)
5. **The Ripcord** (pull-to-transfer-to-a-human) was already live from HOMEPAGE CANDY — re-verified it
   still works: pull or tap → "Transferring you to the owner now." + buzz + green "HUMAN ON THE LINE" chip → resets.
6. **A Brand Vault** now lives at `/brand/` (private / noindex): a dark gallery of every logo, favicon,
   banner and print asset with download + copy-URL buttons. This is now the single source of truth for the
   logo stack — you can delete the scattered project-file copies.
7. **Consistency sweep + QA:** cost answer matches the Master Sheet, calculator "Copy my results" works,
   every audio link resolves, no leftover old-audio links, no forbidden words/numbers/frameworks, and the
   accessibility + layout-stability scores held.

---

## DONE table

| # | Section | What shipped | Live proof |
|---|---------|--------------|-----------|
| A | Audio recast | AVA re-rendered (Closer settings, eleven_v3) + 7 distinct premium caller voices, 70 line segments + 7 stitched full.mp3 → `/audio/v2/`; refs swapped in `ava-data.js` + JSON-LD + transcripts | Pod streams `/audio/v2/medspa/*.mp3` (206); all 7 `full.mp3` = 200; prod `ava-data.js` = 77 v2 refs, 0 v1 |
| B | Ripcord | (already live) re-verified pull/tap → "Transferring you to the owner now." + vibrate(20) + "HUMAN ON THE LINE" chip → 3s reset; reduced-motion safe | pulled=true, transfer line + chip fired, auto-reset caught in screenshot |
| C | Call-me visibility | White-card call-me pane (visible input, TCPA kept); tab relabeled "📞 Call me live" + green breathing beacon; single-green-pulse-per-viewport coordinator | mini-gate bg = `rgb(255,255,255)`; beacon green + labelled; coordinator suspends other pulses |
| D | Depth pass | Glowing left-edge on FAQ + transcript cards + stat/before-after boxes (cyan) and money boxes (gold: cost FAQ, ROI card, $0 stat); inner gradient; consistent radii; accent law ≤3/screen | edges visible in prod screenshots; `DEPTH PASS` + `money-edge` in prod CSS/HTML |
| E | Micro-candy | Footer link-glow on hover (cyan text-shadow). Audio-reactive header dot + desktop pricing lift/1.5° tilt already live | `footer link-glow` in prod CSS |
| F | Brand Vault | `/brand/index.html` noindex gallery — 25 assets grouped by category, preview + filename + dims + Download + Copy-URL; canon rule + palette + do's/don'ts; `og-home.png` consolidated in | `/brand/` = 200, renders, `noindex,nofollow` present |
| G | Consistency + QA | Cost FAQ = Master Sheet v3; "Copy my results" works; all audio 200; 0 orphan v1 refs; greps clean; a11y 100 / CLS 0.00 | see QA below |

## QA gate

- **Lighthouse mobile:** Accessibility **100**, Best Practices **100**, SEO **100** (60 audits passed, 0 failed).
- **CLS: 0.00** (clean reload trace, confirmed twice). LCP ~441ms unthrottled-live on this run (hero H1 path
  untouched by these changes; vs the throttled 324ms baseline this is measurement-context, not a regression).
- **Contrast ≥AA** on changed panes (white card: `#10131A` on white; consent `#4A5160` on white ~7:1; beacon green on near-black).
- **Greps:** she/her for AVA = 0 · retired 305/480/786 = 0 · frameworks = 0 · only `414-240-8930` (voice) + `350-220-5305` (SMS).
- **Flow (390×844 behaviors):** hear demo ✓ · Call-me live beacon + white card ✓ · ripcord pull → transfer + chip + reset ✓ · `/book` = 200.

---

## Checkpoints / one-line rollback

| Commit | Section | Rollback |
|--------|---------|----------|
| `024e589` | §C Call-me white-card + beacon + coordinator | `git revert 024e589 && git push` |
| `813b31e` | §D depth pass + §E footer glow | `git revert 813b31e && git push` |
| `e36de54` | §F Brand Vault | `git revert e36de54 && git push` |
| `57d737a` | §A Audio recast → /audio/v2/ | `git revert 57d737a && git push` (v1 audio at /assets/audio/ instantly restored) |

To swap any single caller voice: change the `voice_id` in `audio/v2/voices.json`, re-run the render for that
vertical, re-stitch, and re-push. AVA voice + settings are also documented there.

## Caller cast (per vertical)

HVAC = Chris (down-to-earth) · Electrical = Will (relaxed) · Roofing = Roger (laid-back) ·
Corporate Car = Eric (smooth exec) · Plumbing = Bella (warm pro) · Dental = Sarah (reassuring) ·
Med-Spa = Jessica (playful). All American, distinct, no heavy accents.

---

## What's next / gotchas

- **Voice = your ear is final.** Voices were cast from metadata (American, conversational, natural) — I can't
  judge "most human" by ear in a headless run. Give them a listen; any voice is a one-line swap (see above),
  and v1 is one `git revert` away. This is the only judgment call worth your review.
- **Problem before/after clips** (`/assets/audio/problem/today.mp3`, `with-ava.mp3`) were left on v1 by design
  (they're a ring/voicemail teaser, not a full demo call). Say the word to recast them too.
- **ElevenLabs MCP key is dead (401).** Audio was rendered via the **Doppler** `ELEVENLABS_API_KEY` (ava-prod/prd)
  over the REST API. If you regen audio, use Doppler, not the MCP.
- **Brand Vault** is the canonical logo stack now — safe to delete scattered project-file logo uploads.
- **POLISH FREEZE:** homepage is re-frozen at this baseline (`57d737a`). Bug fixes only; new features need an
  explicit un-freeze. (CLAUDE.md still cites "THE FINAL CUT 2026-07-09" as the freeze line — worth updating to
  "THE LAST CUT 2026-07-11" next time you touch it.)
- **LCP** measured 441ms unthrottled-live this run vs the 324ms throttled baseline — not a regression from these
  changes, but worth a Lighthouse re-check from your own network before launch.

## Prompt feedback (how could this prompt be better?)

Strong brief — the additive `/audio/v2/` law + "commit per section" made it clean and safe. Two things would
sharpen it: (1) name the **exact caller voice** (or 2-3 approved IDs) per vertical up front, since "pick the
most human" is an ear call a headless agent can't make — that's the one place I had to choose for you; and
(2) clarify whether the S3 before/after "problem" clips count as "every demo call" (I scoped them out as a
teaser, not a call). Everything else was unambiguous.
