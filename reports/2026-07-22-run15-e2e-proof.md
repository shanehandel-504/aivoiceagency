# RUN 1.5 — END-TO-END LIVE PROOF

**Run:** 2026-07-22 22:2x–22:4x CDT · **Target:** owner cell `+1480***11`
(redacted — read from Doppler `OWNER_CELL_PHONE` at runtime, never written to the repo)

> ## RESULT: FULL LOOP COMPLETED — with one hop unverified (GHL → n8n)

The machine ran the whole way: a form post produced a real text, the READY gate produced a
real phone call from a pool number carrying the prospect's own scraped business data, the
call was **answered and held a 57-second conversation**, and the post-call text landed.

---

## Per-hop latency

| # | Hop | Seconds | Result | Evidence |
|---|---|---:|:--:|---|
| 1 | form POST → `/webhook/live-intake` | 4.99 | ✅ | `{"ok":true}` |
| 2 | TEXT 1 (READY gate) dispatched | 0.60 | ✅ | GHL `messageId=RYEWnTvk9p5ycOdxP2c6` · scrape: *AI Voice Agency / AI receptionist / Kewaskum* |
| 3 | READY reply via GHL | 180.0 | ⚠️ | **No genuine inbound in 180s — leg UNVERIFIED** |
| 3b | READY synthesized → `/webhook/live-ready` | 2.59 | ✅ | `{"ok":true,"call_id":"call_6a399ea3db65f2efb2f5901f60a"}` |
| 4 | outbound call placed | <0.1 | ✅ | from `+14149466486` (DEMO-POOL-03) · **8/8 dynamic variables populated** |
| 5 | call completed | 82.7 | ✅ | `ended` · **57s talk time** · `agent_hangup` |
| 6 | post-call TEXT 2 | 0.9 | ✅ | GHL `messageId=JKumJmYPx22L1FfHCNRS` (after fix #5 below) |
| 7 | duplicate-webhook replay | — | ✅ | second `call_analyzed` → `{"ok":true,"duplicate_or_unknown":true}` — no second text |

**Form submit → text on the phone: ~5.6 seconds. READY → phone ringing: ~2.6 seconds.**

---

## The call, verbatim

Agent `agent_67381fcfabf6731dad4f40c590` (AVA HEAR-IT-LIVE v1), 57s, `agent_hangup`:

```
User:  Hello. Can I help you?
Agent: System test for AI Voice Agency — this is AVA, the agent you just launched
       from the website. How do I sound?
User:  Sound great.
Agent: While we were connecting I read aivoiceagency.ai — you handle AI receptionist
       and answering service in Kewaskum, and I saw Starter four ninety-seven a month
       plus four ninety-seven setup. Did I get that right?
User:  Yeah.
Agent: Here's why I exist: when your line rings at three AM and nobody answers, that
       customer taps the next name on Google. I answer in one ring, book the job on
       your calendar, and text you the details.
User:  Sounds great.
Agent: The second we hang up, check your texts — your setup link is live for forty-eight
       hours. Want me to send it now?
User:  Yes.
Agent: Done — it's in your texts. Talk soon, Shane.
```

**What this transcript proves, line by line:**

- **STAGE_1–5 all fired in order**, verbatim to the installed script.
- **Every dynamic variable resolved from a live scrape** — `company_name`, `website_url`,
  `service_1`, `service_2`, `city`, `scraped_fact`, `first_name`. Not one fell back.
- **The "speak numbers as words" constraint held.** The scraped fact contained the literal
  string `Starter $497/mo + $497 setup`, and AVA said *"four ninety-seven a month plus four
  ninety-seven setup."* No symbols spoken.
- **The STAGE-4 deploy patch held in a real call.** AVA said *"your setup link is live for
  forty-eight hours"* — **not** the doc's dashboard promise. The thing that does not exist
  yet was never claimed to a human.

---

## The one hop this proof does NOT cover

**GHL → n8n (`/webhook/live-ready`) is UNVERIFIED.** No genuine inbound SMS reached n8n
inside the 180-second window, so the READY webhook was synthesized to exercise everything
downstream of it. Every other hop above is real.

**To close it (30 seconds, Shane):** reply `READY` to the AVA text on your phone, then check
that a new execution appears on workflow `V6wAFgJ803xmLM0K`. If none does, the GHL
inbound-message webhook is still not pointed at
`https://circulant.app.n8n.cloud/webhook/live-ready`.

Until that is confirmed, **a real prospect replying READY will get no call.** This is the
highest-priority item left in the machine.

---

## Bugs this proof caught (all fixed, all re-verified)

None of these were visible from reading the code — only from executing it.

| # | Bug | Impact if shipped | Fix |
|---|---|---|---|
| 1 | `new URL()` throws in the n8n Code sandbox | **100% of submissions with a website rejected.** The browser *has* `URL`, so the page tested clean — the failure existed only server-side | regex hostname parsing |
| 2 | data-table `get` returning 0 rows halts the branch | **the first-ever submission dies silently** (empty table), and every READY with no open intake | `alwaysOutputData` on all three lookups |
| 3 | Grok body built as an inline expression | double-escaped regex → `invalid syntax` → no extraction at all | moved HTML→text into a `Strip HTML` Code node |
| 4 | `call_ended` *and* `call_analyzed` both treated as terminal | **every prospect texted twice** after their call | act on `call_analyzed` only, plus a row-level `done` guard |
| 5 | post-call GHL upsert read `$json` (a regression introduced by fix #4) | empty upsert → 400 → SMS 404 `CONTACT_ID_NOT_PROVIDED` → **no post-call text** | Dedupe Guard carries the row's identity fields forward |

---

## Housekeeping

- The throwaway maintenance workflow used to reset a test row was **deleted** — no
  `ZZZ MAINT` workflow remains on the instance.
- `LIVE INTAKE v1` (`V6wAFgJ803xmLM0K`) is **active**, 51 nodes.
- Two test rows remain in the `demos` table (`ChainTest`, `Shane`), both `status=done`, so
  neither counts against the 5-concurrent cap. The owner cell is now inside the
  1-per-number/30-day cap — clear that row before re-running this proof.
