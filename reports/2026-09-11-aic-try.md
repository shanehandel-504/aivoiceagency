# RUN — AIC /try browser demo (2026-09-11)

v1 was fact-checked into v2; v2 shipped the page and a Vercel function that could
only ever answer 503, because that project has no Retell key and only a dashboard
login can add one. v3 moved the secret and the gates into n8n. This is the v3
readback; the v2 one is in git history at `4aa497f`.

```
===== SHANE READBACK — COPY ALL =====
LIVE — aichauffeur.ai/try. Tap the button, allow the mic, and you talk in the
browser to the same AVA that answers (414) 775-0019, on the same published
version. Nothing is left for you to switch on.

WHAT CHANGED SINCE THE LAST READBACK
- The browser no longer calls anything on Vercel. It posts to an n8n webhook that
  holds the Retell key, the gates and the budget. Vercel serves the page only.
- The old Vercel function is deleted. /api/web-call now returns 404.
- Turnstile is out of this build: aichauffeur.ai is not on that widget (Cloudflare
  answers 110200) and Doppler has no Cloudflare token, so nothing here could add
  it without you. In its place the page solves a one-time puzzle before n8n will
  create a call. That is a speed bump; the budget below is the real defence.

WHAT PROTECTS IT, IN ORDER
  1  only aichauffeur.ai may ask
  2  40 calls a day
  3  3 tries per internet connection per hour
  4  the puzzle: about a million hashes, ~0.2 s on a laptop, a few seconds on a
     phone, one-time and expiring after three minutes
  5  it steps aside whenever 10 of the account's 20 call slots are busy, so both
     phone lines keep answering
  6  each browser call is capped at 8 minutes, with a minute of silence ending it

DONE
| What | Live | Proof |
|---|---|---|
| /try page | yes, aichauffeur.ai/try/ | commit 5013e1d, deploy dpl_3boDbfDeURtUkDbQ8DNBHLya9p7P; no sideways scroll at 320/390/430/1440; weakest text contrast 5.98:1; button inside the 390x844 screen; "retell" nowhere on screen |
| n8n workflow | yes | WF-TRY-WEBCALL 9nKn8i2dRuALikuv, published 5eefa885-370f-4d13-9285-0bc94e5af117, versionId == activeVersionId, Error Sentry attached, executions kept |
| Retell key | in n8n only | credential "Retell API" kGVhqVmqLgPUAxpM, header auth, restricted to api.retellai.com; the page and the repo hold no secret |
| Real call, cap proof | yes | call_33583d539e09678fe50b72582af: web call, published v27, tagged aichauffeur.ai/try, ended itself at 60 s with max_duration_reached |
| Real call, clean run | yes | call_dd9527f60e438181dd306138d1e: 25 s, 3 AVA turns + 3 caller turns, live 4 s after the tap, puzzle solved in 2.4 s |
| Nothing leaks | yes | the webhook returns only call_id, access_token, transport, ice_servers |
| Refusals | yes | foreign Origin 403, missing Origin 403, a proof for a nonce never issued 403, then per-IP 403, 403, 403, 429 |
| Nothing was created by those refusals | yes | n8n execution 9414 shows "Create web call" never ran, and Retell's ledger shows no call from any of them |
| Failure paths | yes, in production | in-app banner; mic blocked -> mic line; call library blocked -> phone line; refused proof -> phone line in 0.1 s; 429 -> phone line |
| The phone line is untouched | yes | agent v27 still carries its own 900000 ms cap; the 8-minute cap rides on the web call only |
| /demo | unchanged | still the indexed phone-first demo, with one line linking here; only its sitemap date moved; /try is noindex and not listed |
| Tests | yes | 19/19 on the Code-node bodies and the pure-JS SHA-256 against node:crypto, negative control first; render gates ALL PASS local and production |

IDS + ROLLBACK
- 5013e1d  the v3 page, tools, workflow export, spec   -> git revert 5013e1d
- 419b57b / e4bb3ab  the v2 page and function          -> already superseded
- Workflow 9nKn8i2dRuALikuv, published 5eefa885-370f-4d13-9285-0bc94e5af117.
  Kill switch and rollback: unpublish it. Every tap then lands on the phone line.
- Deploy dpl_3boDbfDeURtUkDbQ8DNBHLya9p7P. Instant page rollback: promote the
  pre-run deploy dpl_CqEUGDyT7bv32ob1UKknabZrK8zH in Vercel.

NEXT — only Shane
1. Send the link and watch it. Nothing needs switching on.
2. Your iPhone Safari test: tap, give your real mobile, say yes to texts. AVA
   should ask for the number, and the text should arrive.
3. Open the link inside the LinkedIn app: the "open in Safari" banner should show.
4. Don't set an 8-minute cap on the agent itself; that would cut off real phone
   callers. The web call carries its own.

GOTCHAS
- The webhook URL is public by nature: it sits in the page. The puzzle, the
  3-per-connection hour, the 40-a-day cap and the 10-slot reserve are what stand
  in front of it. A determined script can solve the puzzle too; what it cannot do
  is exceed the budget.
- The per-connection counter lives in n8n's workflow memory. It resets if n8n
  restarts, and under simultaneous calls it counts loosely. The daily cap and the
  slot reserve are the hard limits.
- Worst case a day costs about 40 calls x 8 minutes, roughly $55, and the phone
  lines still answer because of the 10-slot reserve.
- If you ever add aichauffeur.ai to the Turnstile widget, the bot check can go
  back in front of the puzzle; nothing else needs to change.
- The agent still speaks no recording disclosure, while /privacy says callers are
  told at the start. /try shows "Calls are recorded." above the button, but the
  spoken line is a flow fix for the tuning chat.
- The v28 agent draft (12-minute cap, 60-second silence hang-up) is still
  unpublished. Compare it with v27 in the tuning chat before publishing.
- Retell removes GET /get-agent-versions on Sep 15. tools/codex-read/retell-read.mjs
  "versions" and several other tools use it.
- Two test calls now sit in Retell's log, both tagged aichauffeur.ai/try, 60 s and
  25 s. Neither gave a phone number, so neither reached contact capture.
```
