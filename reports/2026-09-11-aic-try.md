# RUN — AIC /try browser demo (2026-09-11)

```
===== SHANE READBACK — COPY ALL =====
RUN INCOMPLETE — the /try page is live, but calls cannot start until two settings
are in place: both keys in the aichauffeur Vercel project (then a redeploy), and
aichauffeur.ai added to the Turnstile widget. Until then every tap sends the
visitor to (414) 775-0019 in about 0.1 seconds.

WHAT HAPPENED (plain English)
- aichauffeur.ai/try is live. One screen, one button. Tap it, allow the mic, and
  you talk in the browser to the same AVA that answers (414) 775-0019, on the
  same published version.
- The server piece that starts each call is live too. It refuses other websites
  and bots, allows 3 tries per connection per hour, keeps 10 of the account's 20
  call slots free for the real phone lines, and caps each browser call at 8
  minutes (a minute of silence also ends it). The phone line's own settings are
  untouched.
- I placed one real test call through it from this PC. AVA answered, heard the
  test voice, and the call ended itself at the 60-second test cap. That proves
  the whole chain works end to end.
- In production it stops at the first step: Vercel has neither key, and the
  bot-check widget does not allow aichauffeur.ai yet. So the button falls back to
  the phone line, as the brief asked.
- Found and fixed on the way: when the bot check refused, a visitor sat on
  "Connecting" for 20 seconds before seeing the phone line. Now it is 0.1 s.
- /demo was not redirected. It keeps its place and gained one line:
  "No phone handy? Try it in your browser."

DONE
| What              | Live                        | Proof |
|-------------------|-----------------------------|-------|
| /try page         | yes, aichauffeur.ai/try/    | 200, noindex; no sideways scroll at 320/390/430/1440; weakest text contrast 5.98:1; button inside the 390x844 screen; "retell" nowhere on screen |
| /api/web-call     | yes, not configured         | GET 405, other site 403, no Origin 403; POST from the site 503 not_configured (log: retell_key false, turnstile_secret false) |
| Server logic      | tested                      | tools/aic-try/unit.mjs 21/21, negative control first |
| Real call         | tested locally              | call_511e14867afad9d8df953b73aea: web call, v27 (published), 5 AVA + 5 caller turns, tagged aichauffeur.ai/try, ended itself at 60 s (max_duration_reached) |
| Failure paths     | yes, in production          | in-app banner; mic blocked -> mic line; call library blocked -> phone line; server 429 -> phone line; bot-check refusal -> phone line in 0.1 s |
| /demo line        | yes                         | live on aichauffeur.ai/demo/; only /demo's sitemap date moved; /try not listed |
| Site gates        | yes                         | RUN 9 render gate, 16 pages x 6 widths, PASS; RUN 10 gate shows the same 18 older FAQ failures as before, 0 new |

IDS + ROLLBACK
- e4bb3ab  page, function, /demo line, sitemap, tests   -> git revert e4bb3ab
- 419b57b  fast fallback when the bot check refuses      -> git revert 419b57b
- Deploys: dpl_QyyBmHZUVfevTAdQ3L3Qhn7G6ftn (e4bb3ab), dpl_8Tmmp5LcJsvX6HEHwebJJHSbBtFV (419b57b).
  Instant undo of both: promote the pre-run deploy dpl_CqEUGDyT7bv32ob1UKknabZrK8zH in Vercel.
- Ledger: board item aic-try (status pending) + this report + the Notion receipt.

NEXT — only Shane
1. Vercel -> aichauffeur -> Settings -> Environment Variables: RETELL_API_KEY and
   TURNSTILE_SECRET (values from Doppler ava-prod/prd), Production and Preview.
2. Redeploy: Deployments -> the top one -> ... -> Redeploy. Keys only reach deploys
   made after they are added. (Or say GO and I push one.)
3. Cloudflare -> Turnstile -> the widget -> Hostnames -> add aichauffeur.ai.
4. Say GO. I run the live call through the real page, the probes and the 429
   check, flip aic-try to live, and close this out.
5. After that: your iPhone Safari test (real mobile, yes to texts) and the
   LinkedIn-app banner test.

GOTCHAS
- Adding the keys is not enough on its own. Vercel only applies them to new
  deploys, so redeploy after adding them.
- Until the hostname is added, Cloudflare logs a 400 in the browser console on
  every /try visit. Visitors see nothing wrong; the button goes to the phone line.
- The agent still speaks no recording disclosure, and /privacy says callers are
  told at the start. /try shows "Calls are recorded." above the button, but the
  spoken line is a flow fix for the tuning chat.
- Retell removes GET /get-agent-versions on Sep 15 (4 days).
  tools/codex-read/retell-read.mjs "versions" and several other tools use it.
- The test call used Cloudflare's test keys for the bot check only; the agent,
  the audio and the cap were real. It sits in Retell's log as one 60-second web
  call tagged aichauffeur.ai/try.
- The chauffeur-design skill still describes the old cyan site. This run followed
  chauffeur/DESIGN-SYSTEM.md, which is what production uses.
```
