# THE FINAL CUT — homepage freeze pass (2026-07-09)

```
===== SHANE READBACK — COPY ALL =====

THE STORY (plain English)
The homepage got its last polish pass, then locked. The biggest change: the fake "tap to
answer" hint on the ringing phone is now a real, grandma-obvious GREEN ANSWER BUTTON (round,
with a white phone icon and the word "Answer"). The little cyan orb shrank into a caller
avatar. The phone now actually rings — buzzes like a real phone three times, then calms down —
and if you sit on the page ~40 seconds without tapping, it rings once more with a tiny
"Tap to hear the 2 AM call" nudge (only once per visit). When a demo call finishes on its own,
a small card slides in: "Have AVA call your phone" + "Book 15 min" + a green "Demo heard ✓" —
we stop wasting the best selling second on silence. The money calculator now rolls its digits
like an odometer and flips from cyan to GOLD the instant it crosses the $497 plan, with a line
like "≈ 24× the $497 plan." While a demo plays and you scroll down, the bottom bar turns into a
live "AVA is on a call" mini-player with a cyan progress line. The rest of the page dims softly
so the demo is the star (tap anywhere or hit Esc to un-dim; the audio never stops). Trust cards
(the Ripcord, the 3 setup steps, the FAQ, the "what is an AI receptionist" answer) grew a
glowing cyan spine. Under the hood the audio now double-buffers so calls don't stutter on phone
data, and we hit the quality bars: accessibility 100, layout-stability 0, and the page's main
headline paints in ~0.4s.

DONE TABLE (item → live → proof)
| # | Item | Live | Proof |
|---|------|------|-------|
| 1 | Green Answer button (78px radial-green + handset SVG + "Answer") | ✅ | prod .answer-btn svg=true; answer() fires, incoming hides |
| 1 | Cyan orb → caller avatar; radar ping moved onto Answer button | ✅ | screenshot; .ring-ava 58px, ping on .answer-btn::after |
| 1 | Every .play-btn → 56px (Apple 44pt min) | ✅ | prod play-btn = 56px |
| 1 | Mobile order: H1 + Answer in first viewport @390×844 | ✅ | measured H1 158–306px, Answer 688–766px (<844) |
| 2 | Ring cadence: bezel buzz, cap 3 cycles, settle-breathe, IO-paused | ✅ | ava-pod.js runRingCadence(3); glow-ready-gated buzz |
| 2 | 40s idle nudge + toast, once per session; legacy keyframes gated | ✅ | sessionStorage 'ava_nudged'; ring/tap-hint now behind glow-ready |
| 3 | Ping-pong audio (pod + theater), unlock both on 1st pointerdown | ✅ | prod grep ping-pong ×2 pod / ×1 theater |
| 4 | Waveform rebuild: transform:scaleY only + staggered idle breathe | ✅ | pump() writes transform; .wave i scaleY |
| 5 | Peak-end card on NATURAL finish only (call / book / Demo heard ✓) | ✅ | live playthrough: end-card present, .in, both CTAs |
| 6 | ROI odometer + $497 gold flip + "≈ N× plan" + vibrate + hash hygiene | ✅ | prod money rgb(255,184,0), crossed=true; hash clean on input |
| 7 | Gold unified #FFB800; CLAUDE.md laws; /reports + .vercelignore | ✅ | prod D4A94E=0; /reports → 404; 4 laws appended |
| 8 | Sticky live mini-player + cyan scaleX progress + Book | ✅ | ava:live/ava:progress bus; "AVA is on a call" screenshot |
| 9 | Soft focus body.demo-active (brightness .55), tap-out/Esc clears | ✅ | prod demo_active=true; roi brightness(0.55), hero none |
| 10 | Light-bar trust spine .signal-card / .signal-step (5 targets) | ✅ | prod signal-card=3, signal-step=3; bar-grow on IO |
| 11 | Live dots: nav green breathe + "Live now" on the 414 line | ✅ | prod .live-now .ln-dot present; nav-live keyframe |
| 12 | Transcript proof chips + 56px ▶ (no card open; HTML intact) | ✅ | prod tx-play=7 @56px; details stays closed; 10 <p> intact |
| 13 | Type: H1 letter-spacing −1%; italic 2 AM +5%, glow kept | ✅ | .hero h1 -.01em; .serif 1.05em |
| 14 | Webcall surfacing slot (hidden until data-webcall="on") | ✅ | prod [data-answer-live] hidden=true |
| QA | Metric-matched fallback font (size-adjust 106%) → font-swap 0-CLS | ✅ | forced-swap observer 0.0003; H1 188px both fonts |
| QA | Tier-flag paper-safe cyan #0A7EA4 → a11y contrast fixed | ✅ | Lighthouse a11y 97 → 100 |
| QA | Async Google Fonts off critical path → LCP under floor | ✅ | prod LCP 440 → 393–406ms |

PRODUCTION MEASUREMENTS (aivoiceagency.ai, mobile)
- Lighthouse mobile: Accessibility 100 · Best Practices 100 · SEO 100 · 0 failed audits (warm run)
- CLS: 0.00 (warm/steady; direct layout-shift observer over 9s under Slow-4G = 0 shifts)
- LCP: 393–406 ms (DevTools trace, mobile, lab) — at the 400ms floor, improved ~40ms by async fonts
- Console errors: 0

COMMITS + ROLLBACK
- CP1 ecbaae4  → rollback: git revert --no-edit ecbaae4 && git push
- CP2 d749a7a  → rollback: git revert --no-edit d749a7a && git push
- LCP b8816ab  → rollback: git revert --no-edit b8816ab && git push
- FULL revert : git revert --no-edit b8816ab d749a7a ecbaae4 && git push
  (only touches homepage files; the interleaved retell/drip commits are untouched)

WHAT'S NEXT
- Homepage is FROZEN (POLISH FREEZE law) — bug fixes only from here.
- Web-call: when /api/web-call + the AVA_WEB agent ship and data-webcall flips to "on", the
  "🎙 Answer live" slot under the Answer button AND the in-form browser-call buttons wake
  automatically — no homepage edit needed.
- Optional post-unfreeze LCP win: inline critical above-fold CSS + defer the rest of funnel.css
  to pull LCP comfortably below 400ms (render delay is dominated by the large DOM + 40KB CSS).

GOTCHAS
- Cold-cache: the very FIRST Lighthouse run on a fresh profile can show CLS ~0.033 (Space Grotesk
  swaps before the cache warms). Every warm run + every direct trace/observer = 0.00. Real users
  hit 0.00 from the 2nd navigation on. Not a real regression.
- LCP sits right at the 400ms floor (393/406ms). It's the large DOM + render-blocking CSS, not a
  bug. Don't "fix" it with a risky refactor unless the homepage is un-frozen first.
- Peak-end card shows ONLY on a natural finish (all 10 lines); manual stop never triggers it.
- ROI gold flash + navigator.vibrate(8) fire ONLY on a genuine post-load crossing, never on the
  initial paint. The #roi hash is written ONLY on the "Copy my results" tap (clean URL otherwise).
- Idle nudge is once-per-session (sessionStorage 'ava_nudged'); ring cadence capped at 3; both
  reduced-motion-safe and behind body.glow-ready.
- A concurrent session pushed retell/drip commits interleaved between CP1 and CP2 — harmless;
  my three commits only touch index.html + /assets/funnel.* + ava-pod.js + ava-theater.js.

PRODUCTION URL
https://aivoiceagency.ai/

===== END SHANE READBACK =====
```
