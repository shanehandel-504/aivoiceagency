# THE MINE — Run Report (2026-07-13)

**Mission:** L1 "THE MINE" — truth sweep · performance · foundation · 16 new pages.
**Rollback tag (whole run):** `pre-mine-2026-07-13`

## Plain-English summary
The website was making promises it can't guarantee ("catches every call", "200+ calls at once", "you won't notice it's AI"), plus two blog claims about customers and results that were never real. All of that is now gone or rewritten honestly — checked page by page by a second AI reviewer, not just a keyword search. The homepage also loads much faster on phones (the important "how fast does it look ready" number dropped from 2.66 seconds to 1.5). And the three Wisconsin city pages (Milwaukee, Madison, Green Bay) were rewritten so each one is genuinely about that city — real neighborhoods, the right area code, a season-by-season call map — instead of the same page with the city name swapped. All of that is **live now**. The last part — 16 brand-new pages with a full visual design — is set up (design system built) but not yet built out; that's the next work session.

## DONE TABLE

| Wave | Artifact | Live? | Proof / ID |
|---|---|---|---|
| 1 | Truth sweep — 59 claim fixes across 24 pages (22 named + 37 adversarial-audit) | ✅ LIVE | commit `672e2d8`; every banned string greps 0; 70 JSON-LD blocks valid |
| 1 | Phone Law 5 (305/350/480/786) | ✅ clean | 0 in public HTML |
| 1 | Self-hosted Space Grotesk (1 variable woff2, preloaded) + defer ava-data.js + cache headers | ✅ LIVE | `/fonts/space-grotesk.woff2` 200 / immutable; `vercel.json` |
| 1 | Mobile LCP 2,660ms → **1,497ms** (gate ≤2,500), CLS 0.00 | ✅ | prod DevTools trace, Slow-4G/4×CPU |
| 2 | `/data/wisconsin.json` — sourced 6-city local-fact spine + 4-season map | ✅ LIVE | commit `f0debb8` |
| 2 | Milwaukee / Madison / Green-Bay hubs rewritten — **swap test PASS** | ✅ LIVE | 200; season map + labeled scenario + /roi "your numbers" + source notes live; 3 JSON-LD blocks/hub valid, 5 FAQs each |
| 3 | `guide.css` design system (Templates A/B/C components, on CIRCULANT tokens) | 🟡 built, not deployed | `assets/guide.css` |
| 3 | 16 new pages + favicon set + 53 OG cards + nav/footer/sitemap + de-orphan landers | ⬜ NOT STARTED | — |

## Checkpoints / rollback
- `pre-mine-2026-07-13` — full-run rollback tag.
- Wave 1: `git revert 672e2d8`
- Wave 2: `git revert f0debb8`
- Waves are independent; reverting one does not affect the others.

## Performance detail (homepage, mobile, Slow-4G / 4×CPU lab)
- Baseline: LCP 2,660 ms (render-delay 2,595 ms = 97%), CLS 0.00.
- After Wave 1: **LCP 1,497 ms**, CLS 0.00. Font round-trip removed + the lone render-blocking script deferred did it — critical-CSS surgery on the frozen homepage was **not needed** to clear the gate.
- PSI composite score not captured: public PSI API daily quota exhausted (HTTP 429). Confirm at pagespeed.web.dev when quota resets.

## Wave 3 — remaining build (resume plan)
**Inputs in hand:** ChatGPT master file (`~/Downloads/CHAT GBT 5.6 … 2026-07-13 21_14 UTC.md`) — 4 verbatim master pages (comparison / cost / Kenosha / math), shared block library, swap test, release gate, claims guard, 15 titles+metas. Copy supplement — moat page (`/done-for-you-vs-diy-ai-receptionist`) full copy + `/ai-receptionist-cost` body spec + corrected pricing math (flat only beats metered above ~200 calls/mo → concede price, win on done-for-you). Design supplement — Templates A/B/C, the terminal call component (signature move), favicon + OG system.
**Foundation done:** `assets/guide.css` (all components).
**Remaining, in order:**
1. Favicon completion: generate `favicon-16.png`, `favicon-32.png`, `android-chrome-192/512.png` (+ maskable) from `favicon.svg` via ImageMagick `convert`; write `site.webmanifest`; link on all pages. (favicon.svg/.ico/apple-touch-icon/og-image already exist.)
2. Build the 16 pages against `guide.css` + a strict head/schema skeleton (verbatim copy where the master file provides it: comparison #1, cost #5, Kenosha #10, math #4; supplement: moat #16, cost #6; author the other 10 from architecture + shared blocks). Each: 40–60-word answer-card, question-H2s, FAQPage JSON-LD (4+ visible), terminal call example labeled SAMPLE DATA, BreadcrumbList + BRIDGE nav/footer/crumb/callbar, dateModified.
3. OG cards ×53 via SVG→PNG (`convert`), ≤300KB each; wire og:image per page.
4. Wire: Guides ▾ nav dropdown (stamp on 53 pages) + footer regen + `sitemap.xml` (53 URLs, lastmod 2026-07-13) + Organization/LocalBusiness schema (414 only) + homepage objection link (`Why is AVA $497 when other AI receptionists start at $29? →` → moat page) + de-orphan the 15 city-trade landers (breadcrumb parent = statewide trade page, link up/side, one unique seasonal block each).
5. Verify: Lighthouse mobile on 2 guides + 1 city (Perf ≥90 / A11y ≥95 / SEO 100), swap test on new city pages, FAQPage JSON-LD validates, every table collapses ≤700px, competitor prices carry a source link + "checked 2026-07-13", no competitor trademark in any <title> or URL, homepage→moat link live. Then deploy + verify live.

## Gotchas / decisions (for whoever resumes)
- **Cache headers deviation:** `/fonts` + `/audio` are `immutable`; `/assets` + `/js` are 1-day + 7-day stale-while-revalidate — because `funnel.css`/`funnel.js` have **unhashed filenames**, blanket `immutable` would serve stale JS/CSS after every deploy.
- **Sequencing:** lander de-orphaning was moved from Wave 2 into Wave 3 so its "link up to the statewide trade page" targets exist first (no 404 links).
- **`.claude/worktrees/*`** holds 5 stale full-site copies from prior agent runs — exclude from all greps; candidate for cleanup (not part of this run).
- **Space Grotesk is a variable font** — Google returns ONE woff2 for all weights; self-hosted as a single `font-weight:300 700` @font-face.
- **CLAUDE.md vs this prompt on `350-220-5305`:** CLAUDE.md calls it the published SMS line (footer only); this run's Law 5 says purge it. It's in **0** HTML today, so moot — but the two rules disagree; Shane's call.
- Competitor prices for the cost/moat pages **must be re-verified against live pricing pages immediately before publishing** and stamped "Prices checked 2026-07-13"; drop any row that can't be reached.

## Master workflow scripts (reusable / resumable)
- `mine-claims-audit` (semantic truth audit, 37 pages) — `…/workflows/scripts/mine-claims-audit-wf_602ebcaa-c65.js`
- `mine-truth-apply` (apply 37 fixes) — `…/mine-truth-apply-wf_0bd1b2db-6ea.js`
- `mine-wave2-hubs` (hub rewrites) — `…/mine-wave2-hubs-wf_169e2c83-dc9.js`
