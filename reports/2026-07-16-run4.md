# RUN 4 — SEO TEETH + AUDIO WALL

**Status:** ✅ LIVE on https://aivoiceagency.ai
**Source / stamp:** `b44afb4` · prod `?v=b44afb4`
**Rollback:** `git revert` the RUN 4 commits, or `git reset --hard 1222aec` (pre-RUN-4 HEAD)
**Shipped:** 2026-07-17 (run opened 2026-07-16)

---

## Plain-English summary (for a non-coder)

Three things shipped, and I checked the site did the reverse of the mission's assumption first — most of the "SEO teeth" were **already built**, so I verified them and only added what was genuinely missing.

1. **The "You're booked" page now has a wall of AVA's real sample calls.** After someone books, they used to see one sample call. Now they see all seven (plumbing, HVAC, electrical, dental, roofing, corporate car, med spa) as a tidy grid they can play right there. Only one has a written transcript today (plumbing) — I did **not** invent transcripts for the others.
2. **Our own visits (and test robots) no longer pollute the analytics.** The tracking code now stays silent for automated browsers, for any device we flag, and for any link ending in `?notrack=1`. Real customers are completely unaffected — proven live.
3. **Every city-trade page got a localized "captured call" ticket** under its headline (e.g. "2:14 AM · Newburg, Washington County — well pump dead"), plus small schema/pricing polish. The bigger SEO items the mission asked for (unique headlines, structured data, canonical tags, internal links) were **already in place** — I audited all 24 pages and confirmed it.

Everything is live and verified on the real site.

---

## DONE table

| # | Artifact | Where | Live? | Proof |
|---|----------|-------|-------|-------|
| 1 | `/booked` "Hear AVA work" audio wall (7 samples) | `booked/index.html` + `.bk-wall` CSS | ✅ | PROD: 7 clip cards, electrical clip **plays** (dur 46.5 = ffprobe), audio `200 audio/mpeg`, 0 h-overflow, 0 console errors |
| 2 | Tracking self-tag (webdriver / `ava_internal` / `?notrack=1`) | `js/tracking.js` | ✅ | PROD 3-state: baseline loads gtag+fbq, `?notrack=1` → none + `ava_internal=1`, persisted flag → none; deployed file carries webdriver branch |
| 2b | Gate scripts append `?notrack=1` | `tools/e2e.mjs`, `render-audit.mjs`, `ring-proof.mjs`, `run3-verify.mjs` | ✅ | `nt()` query-merge helper; all pass `node --check` |
| 3 | Localized captured-call ticket ×15 city-trade | 15 `*/index.html` + `.cap-ticket` in `styles.css` | ✅ | PROD: present on city-trade pages, absent on green-bay hub, JSON-LD still parses, 0 h-overflow @375 |
| 3b | `url = canonical` added to 6 `Service` schemas | 5 vertical hubs + `wisconsin-limo` | ✅ | All JSON-LD parses; url matches canonical |
| 3c | Pricing anchor "Setup takes days on your existing line" | `overview.html` tier note | ✅ | Only non-index page with tier CTAs; local pages have a `$497` **stat**, not a tier CTA → N/A there (logged) |
| 4 | Cache-armor re-stamp | 37 pages + 2 version-only | ✅ | `?v=b44afb4` sitewide (was `5a22ae5`) |
| 5 | Board flip + ISO log | `hq/board.json` | ✅ | New log entry + `run4-audio-seo` item + L1 note + `updated` |

---

## Commits / one-line rollback per checkpoint

| Commit | What | Rollback |
|--------|------|----------|
| `066c19e` | feat: /booked audio wall | `git revert 066c19e` |
| `989c9da` | feat: tracking self-tag + gate scripts | `git revert 989c9da` |
| `b44afb4` | feat: SEO teeth (tickets + schema url + overview anchor) | `git revert b44afb4` |
| `481dc90` | chore: stamp `?v=b44afb4` | `git revert 481dc90` |
| Whole run | — | `git reset --hard 1222aec` (pre-RUN-4) |

---

## What's next / open

- **Transcripts for the other 6 samples** — only plumbing has a written transcript. The other 6 could be transcribed (ElevenLabs STT) if you want full transcript toggles across the wall; deliberately **not** fabricated this run.
- **Google Ads AW- id** still `AW-PENDING` — the tracking self-tag doesn't change that; it's still the 2-constant one-line swap in `js/tracking.js`.
- **Opt this Dell out permanently:** visit any page once with `?notrack=1` (it persists `ava_internal=1` in that browser's localStorage) — do it per browser you use.
- **.gitignore is thin** (`.env` only) — the `git add -A` near-miss below argues for adding `__pycache__/`, `*.pyc`, `*.bak`, `*.backup`, `.ava_build_tmp/`, and `voice-stack/**` build artifacts. Not done this run (out of scope); flagged for your call.

---

## Gotchas

- **Near-miss caught before push:** an accidental `git add -A` on the stamp commit swept in ~42 untracked audit PNGs, 10 old reports, `.pyc`/`.bak`/`.ava_build_tmp`, and `voice-stack/agent/livekit.toml` + `agent.py.backup` (which can carry infra keys — CLAUDE.md forbids keys in commits). Soft-reset the commit and re-committed with `git add -u` (already-tracked files only). Junk restored to untracked; nothing sensitive shipped.
- **Playwright browsers not installed** on this machine (`npx playwright install` needed) — the e2e/gate scripts can't run locally here. Verified every surface directly in the in-app browser against **PROD** instead.
- **Mission vs. reality:** RUN 4 was written as if the SEO teeth needed building from scratch. They didn't — the 15 city-trade + 4 city-hub + 5 vertical-hub pages already had unique H1s, LocalBusiness/Service/Breadcrumb/FAQ schema, canonicals, unique meta descriptions, and hub/roi/book internal links. Per the mission's own "do NOT trust this prompt for the page list," I audited and only added the genuinely-missing pieces (tickets, 6 schema `url`s, overview anchor).
- **`?trade=` deep-links preserved:** the gate-script `nt()` helper appends `&notrack=1` after an existing query and before the `#hash`, so `/?trade=plumbing#stage` → `/?trade=plumbing&notrack=1#stage` (backstage.js still reads `trade`).

===== SHANE READBACK — COPY ALL =====

RUN 4 shipped three things to aivoiceagency.ai (all LIVE, prod-verified, `?v=b44afb4`):

1. **/booked audio wall** — the thank-you page now shows all 7 AVA sample calls as a playable grid (was 1). Plays on prod, no errors, no layout overflow. Only plumbing has a written transcript (didn't fake the other 6).

2. **Tracking self-tag** — our own traffic + test robots no longer hit GA4/Meta/Ads. Silenced when the browser is automated (`navigator.webdriver`), when a device is flagged (`localStorage ava_internal='1'`), or on any `?notrack=1` link (which also remembers the choice). Real customers unaffected — proved all three states live. Test/audit scripts now self-append `?notrack=1`.

3. **SEO teeth** — I audited all 24 local/hub pages first: unique headlines, schema, canonicals, meta descriptions, and internal links were **already there**. So I added the missing pieces: a localized "captured call" ticket under the hero on all 15 city-trade pages (e.g. "2:14 AM · Newburg, Washington County — well pump dead"), added the page URL to 6 schema blocks that lacked it, and added "Setup takes days on your existing line" under the /overview pricing tiers. Local pages have no tier buttons (just a $497 stat), so the pricing line is N/A there; index.html stayed frozen.

DONE (artifact → live):
- /booked 7-sample audio wall → LIVE (plays on prod)
- tracking.js self-tag + 4 gate scripts → LIVE (3 states proven on prod)
- 15 city-trade captured-call tickets → LIVE
- 6 Service schema url=canonical → LIVE (all parse)
- /overview pricing anchor → LIVE
- ?v=b44afb4 re-stamp (39 pages) + board.json flip → DONE

Commits: 066c19e (wall) · 989c9da (tracking) · b44afb4 (seo) · 481dc90 (stamp).
Rollback whole run: `git reset --hard 1222aec`.

Live URL: **https://aivoiceagency.ai/booked** (the audio wall) · any city-trade page for a ticket (e.g. https://aivoiceagency.ai/west-bend-plumbing).

Next / your call: (a) transcribe the other 6 samples if you want full transcripts; (b) Google Ads AW- id still pending; (c) visit once with `?notrack=1` on each browser you use to opt your own devices out; (d) consider hardening `.gitignore` (a `git add -A` near-miss almost committed voice-stack backups that can hold keys — caught and reverted before push).

===== END =====
