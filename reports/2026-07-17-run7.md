# RUN 7 — NIGHT EDITION · 2026-07-17

**Status:** ✅ LIVE (Step 3 hero slot skipped: XRAY PACK NOT FOUND — everything else shipped).
**Source commit:** `d7c20e7` · **Stamp/staging commit:** `febdafe` · **Prod cache token:** `?v=d7c20e7`
**Rollback tag:** `pre-run7-2026-07-17` = `63cd761` · **RUN 6 WIP preserved:** unpushed branch `run6-wip` = `3aa8a2b`

---

## Plain-English summary (for a non-coder)

Four things happened tonight. First, everything from the unfinished RUN 6 (the chat widget work,
the pricing-ladder edits) was safely parked on its own branch — nothing was lost and nothing from
it went live. Second, the /lsa ad page no longer invites anyone to text: every button now says
"Call (414) 240-8930" and dials the phone, which keeps the ad lane safe while text-message
registration (A2P) is pending. Third, two new Google-facing help articles went live — "Why does my
furnace keep turning on and off?" and "Why is my AC running but not cooling?" — each answers the
homeowner's question honestly up top, tells them when it's a real emergency, and then turns to the
contractor reading over their shoulder: every one of these searches at night is a job someone's
voicemail is losing, and AVA answers for a flat $497. Both are in the sitemap and the site footer.
Fourth, a private preview page (aivoiceagency.ai/staging/xray.html) now holds a homepage candidate
with a new "Google Health Check" — five yes/no questions about how your phone line actually behaves,
with a live red/amber/green score. Search engines can't index it, nothing links to it, and it swaps
into the real homepage only when Shane says GO SWAP. The planned "Transcript X-Ray" hero could not
be built because the design pack isn't in the repo or uploads — Shane needs to drop it.

---

## DONE — what shipped

| Artifact | Live URL | Proof |
|---|---|---|
| RUN 6 WIP preserved | branch `run6-wip` (NOT pushed) | SHA `3aa8a2b` · main clean at `63cd761` before edits |
| /lsa call-only flip | https://aivoiceagency.ai/lsa/ | ✅ 200 · prod grep `sms:` = 0 · both CTAs tel-only |
| Furnace guide | https://aivoiceagency.ai/guides/furnace-short-cycling.html | ✅ 200 · 3 JSON-LD parse · 390 clean |
| AC guide | https://aivoiceagency.ai/guides/ac-running-but-not-cooling.html | ✅ 200 (after ~60s edge stagger) · 3 JSON-LD parse · 390 clean |
| Sitemap +2 | https://aivoiceagency.ai/sitemap.xml | ✅ 57 URLs, XML valid |
| Footer Insights += guides | site-wide (stamp.py) | ✅ 2 guide links live on prod homepage footer |
| Health-check GA4 spine | js/tracking.js | ✅ `health_check_complete` CustomEvent forwarder, NOTRACK-safe, `node --check` clean |
| X-Ray staging candidate | https://aivoiceagency.ai/staging/xray.html | ✅ 200 · `noindex,nofollow` live · no sitemap · linked nowhere · no chat widget |
| X-Ray hero | — | ⚠️ **XRAY PACK NOT FOUND — Shane drop the pack** (repo design/·handoff/·xray + uploads + Downloads all searched) |
| Board flip + ISO log | https://aivoiceagency.ai/hq | ✅ this commit |

## What the Google Health Check does

- 5 owner questions: answered after 6 PM? · weekends? · under 30 seconds every ring? ·
  Google messages inside 4 hours? · listed hours you can actually cover?
- Live score: 5/5 **CLEAR** Booked-Green `#2EE6A8` · 3–4 **MIXED** Amber `#FFB020` ·
  0–2 **AT RISK** Miss-Red `#FF3B4E` (CIRCULANT-X semantics).
- Engagement-signals language only — describes what a phone line "gives off," promises nothing
  about rankings.
- Vanilla JS, no storage (stated in the UI), fixed-height readout = zero CLS, no decorative motion.
- On the 5th answer the page dispatches `ava:health_check_complete` once; the tracking spine
  forwards it as GA4 `health_check_complete {page, score, of}` through `ga()` — silenced by the
  NOTRACK self-tag, and path-free so it keeps working after GO SWAP moves the section to `/`.
- CTA pair: tel 414-240-8930 + /book.

## Guide anatomy (both pages, unique copy each)

Question-led H1 → bolded 2–3 sentence direct answer (AI-Overview bait) → 5 cause/fix cards in
plain language (qualitative on purpose — zero invented numbers) → "when it's an emergency" safety
list (gas → leave and call the utility; CO alarm → get out first) → sourced after-hours stat set
ONLY (AgentZap 47% / 73%-higher · ServiceTitan 10–14.1% · ~50→~3 shops 2 PM vs 2 AM) → homeowner
10 PM CTA ("call a 24-hour pro… the one that picks up") → contractor block ("Own these calls —
every symptom search tonight is a job someone's voicemail is losing" + $497 flat + /book +
414-240-8930) → FAQ (mirrors the FAQPage schema verbatim) → cross-links (sibling guide,
/hvac-answering-service, /24-hour-answering-service). Article + FAQPage + BreadcrumbList schema;
canonical = the `.html` URL (same root-file pattern as /methodology.html); built entirely from
existing CIRCULANT classes — zero styles.css change.

## Gates

- `node --check` clean: js/tracking.js + the extracted health-check inline script.
- JSON-LD parses: guides (Article/BreadcrumbList/FAQPage ×2) + staging (@graph) + board.json valid.
- In-browser 390×844 on /lsa, both guides, /staging/xray: 0 horizontal overflow, 0 console errors,
  nav/crumb/footer/callbar present on guides, health check interaction-proven (score 3 → MIXED →
  event fired exactly once; re-answers do not re-fire; color cascade verified — the one gray read
  was the embedded browser pane pausing CSS transitions while backgrounded, not a bug).
- Copy laws: only 414-240-8930 (zero 305/480/786/350/775/409) · $497 only outside overview ·
  zero "locked" (word-boundary grep; "blocked/clogged" false-positives cleared) · zero
  "100 free minutes" / AVA she-her / booked-confirmed-guaranteed claims.
- index.html POLISH FREEZE respected: diff = `__ASSET_V` + the 2 sanctioned shared-footer links +
  `?v` tokens only.
- Prod: all RUN 7 URLs 200 · /lsa sms-free · staging noindex live · sitemap 57.

## Checkpoints / rollback

| Checkpoint | ID | Rollback |
|---|---|---|
| Pre-run tag | `pre-run7-2026-07-17` = `63cd761` | `git reset --hard pre-run7-2026-07-17` (then push) |
| RUN 6 WIP | branch `run6-wip` = `3aa8a2b` | it is the backup — do NOT delete; merge later |
| Feat commit | `d7c20e7` | `git revert d7c20e7` |
| Stamp + staging | `febdafe` | `git revert febdafe` (or re-run stamp.py after any revert) |

## What's next

- **Shane:** drop the Transcript X-Ray Design handoff pack → the hero builds into /staging/xray.html.
- **GO SWAP** (Shane's call only): X-Ray hero + health check ride into `/`.
- **RUN 6 merge:** chat widget + pricing ladder come off `run6-wip` when the widget lane is proven.
- Optional: footer "By city & trade" still lacks Green Bay + Waukesha columns (deferred since RUN 5).

## Gotchas

- The health-check band colors read gray in the embedded browser pane after interaction — the pane
  pauses CSS transitions when backgrounded (same reason its screenshot timed out). `transition:none`
  probe showed the correct green; real visible tabs animate normally.
- Vercel edge stagger struck again: the AC guide 404'd for ~60s post-push, then 200 — same as
  RUN 3/RUN 5. Retry before panicking.
- /lsa line 505 carries a pre-existing Shane-authored `<!-- LOCKED STATS -->` HTML comment — it
  greps as "locked" but is not rendered copy and sits under the /lsa design freeze; left untouched.
- `git add -A` was SAFE this run only because `.gitignore` now swallows voice-stack/backups and
  `tools/create_chat_agent.py` was secret-scanned first (env-read only). Keep scanning before -A.
