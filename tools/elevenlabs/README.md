# gen_brian.py — local ElevenLabs run

1. `export ELEVENLABS_API_KEY=your_key_here`
2. `python3 tools/elevenlabs/gen_brian.py` (run from the repo root, or anywhere — output path is resolved relative to the script)
3. MP3 lands at `outputs/brian-message.mp3` (repo root).
