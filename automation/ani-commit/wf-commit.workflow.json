{
  "name": "WF-COMMIT — AI Chauffeur Reservation Commit v1.0",
  "nodes": [
    {
      "id": "n_hook",
      "name": "Commit Webhook",
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 2.1,
      "position": [
        -900,
        0
      ],
      "webhookId": "commit-reservation-run2",
      "parameters": {
        "httpMethod": "POST",
        "path": "tools/commit-reservation",
        "authentication": "none",
        "responseMode": "responseNode",
        "options": {}
      }
    },
    {
      "id": "n_verify__normalize",
      "name": "Verify + Normalize",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        -700,
        0
      ],
      "parameters": {
        "jsCode": "// WF-COMMIT · node \"Verify + Normalize\"\n// Auth identical to WF-RATE. Then: validate RESERVATION_CORE + the trip-type extension,\n// mint the trip id, hash the canonical payload, and build the GHL write payloads.\nconst crypto = require('crypto');\n\nconst item = $input.first().json || {};\nconst headers = item.headers || {};\nconst body = item.body || {};\nconst SECRET = ($vars && $vars.RATE_SHARED_SECRET) || '';\n\nconst hdr = (k) => {\n  const lk = String(k).toLowerCase();\n  const keys = Object.keys(headers);\n  for (let i = 0; i < keys.length; i++) {\n    if (String(keys[i]).toLowerCase() === lk) return String(headers[keys[i]] == null ? '' : headers[keys[i]]);\n  }\n  return '';\n};\nconst ctEq = (a, b) => {\n  if (typeof a !== 'string' || typeof b !== 'string') return false;\n  const ba = Buffer.from(a, 'utf8'); const bb = Buffer.from(b, 'utf8');\n  if (ba.length !== bb.length) return false;\n  return crypto.timingSafeEqual(ba, bb);\n};\n\nlet auth_ok = false, auth_mode = 'none', auth_reason = '';\nif (!SECRET) {\n  auth_reason = 'RATE_SHARED_SECRET is not set in n8n Variables';\n} else {\n  const sig = hdr('x-retell-signature'); const shared = hdr('x-rate-secret');\n  if (sig) {\n    auth_mode = 'hmac';\n    const expected = crypto.createHmac('sha256', SECRET).update(JSON.stringify(body)).digest('hex');\n    const parts = String(sig).split(/[,;\\s]+/);\n    for (let i = 0; i < parts.length; i++) {\n      const p = parts[i]; if (!p) continue;\n      const eq = p.indexOf('=');\n      const cand = eq >= 0 ? p.slice(eq + 1).trim() : p.trim();\n      if (ctEq(cand.toLowerCase(), expected)) { auth_ok = true; break; }\n    }\n    if (!auth_ok) auth_reason = 'x-retell-signature did not match HMAC-SHA256 of the request body';\n  } else if (shared) {\n    auth_mode = 'shared'; auth_ok = ctEq(shared, SECRET);\n    if (!auth_ok) auth_reason = 'x-rate-secret did not match the shared secret';\n  } else {\n    auth_reason = 'neither x-retell-signature nor x-rate-secret was supplied';\n  }\n}\n\nconst args = body.args || body.arguments || body.parameters || body;\nconst call = body.call || {};\nconst R = args.reservation || args.Reservation || {};\n\nconst str = (v) => (v === undefined || v === null) ? '' : String(v).trim();\nconst numOrNull = (v) => {\n  if (v === undefined || v === null || v === '') return null;\n  const n = Number(v); return isNaN(n) ? null : n;\n};\nconst nn = (v) => { const s = str(v); return s === '' ? null : s; };\n\n// --- identifiers -----------------------------------------------------------\nconst tenant_id = str(args.tenant_id || body.tenant_id || (R.ids || {}).tenant_id) || 'demo';\nconst call_id = str(args.call_id || body.call_id || call.call_id || (R.source || {}).call_id);\nconst intake_id = str(args.intake_id || body.intake_id || (R.ids || {}).intake_id);\nconst quote_id = nn(args.quote_id || body.quote_id);\n// Idempotency key. The brief names retell_call_id; call_id is the same value on the\n// Retell payload, so either spelling resolves to one key.\nconst retell_call_id = str(args.retell_call_id || body.retell_call_id || call_id);\n\n// --- RESERVATION_CORE ------------------------------------------------------\nconst p = R.passenger || {};\nconst veh = R.vehicle || {};\nconst pick = R.pickup || {};\nconst drop = R.dropoff || {};\nconst flight = R.flight || {};\nconst billing = R.billing || {};\nconst booker = R.booker || {};\nconst src = R.source || {};\n\nlet mobile = str(p.mobile_e164 || p.phone);\nlet d = mobile.replace(/[^0-9]/g, '');\nif (d.length === 10) d = '1' + d;\nmobile = d ? ('+' + d) : '';\n\nconst stopsIn = Array.isArray(R.stops) ? R.stops : [];\nconst stops = [];\nfor (let i = 0; i < stopsIn.length; i++) {\n  const a = str((stopsIn[i] || {}).address);\n  if (a) stops.push(a);\n}\n\nconst res = {\n  trip_type: str(R.trip_type).toUpperCase(),\n  first: str(p.first), last: str(p.last), mobile_e164: mobile,\n  email: str(p.email).toLowerCase(), pax_count: numOrNull(p.pax_count),\n  vehicle_class: str(veh.class).toUpperCase(), luggage_count: numOrNull(veh.luggage_count),\n  pickup_datetime: str(pick.datetime_local), pickup_tz: str(pick.tz) || 'America/Chicago',\n  pickup_address: str(pick.address), pickup_notes: str(pick.notes),\n  dropoff_address: str(drop.address), stops: stops,\n  hours_booked: numOrNull(R.hours_booked),\n  airline: str(flight.airline), flight_number: str(flight.number),\n  meet_style: str(flight.meet_style).toUpperCase(),\n  billing_type: str(billing.type).toUpperCase(),\n  booker_name: str(booker.name), booker_company: str(booker.company),\n  special_notes: str(R.special_notes),\n  consent: src.consent === true || String(src.consent).toLowerCase() === 'true',\n  agent_id: str(src.agent_id), recording_url: str(src.recording_url)\n};\n\n// --- validation: core + per-trip-type extension ----------------------------\nconst missing = [];\nif (!res.trip_type) missing.push('trip type');\nif (!res.first) missing.push('first name');\nif (!res.mobile_e164 || !/^\\+[1-9]\\d{7,14}$/.test(res.mobile_e164)) missing.push('mobile number');\nif (!res.pickup_datetime) missing.push('pickup date/time');\nif (!res.pickup_address) missing.push('pickup address');\nif (!res.vehicle_class) missing.push('vehicle class');\nif (res.pax_count === null) missing.push('passenger count');\n\nconst TT = ['HOURLY', 'ROADSHOW', 'AIRPORT_ARR', 'P2P'];\nif (res.trip_type && TT.indexOf(res.trip_type) < 0) missing.push('a recognized trip type');\nif (res.trip_type === 'AIRPORT_ARR') {\n  if (!res.airline) missing.push('airline');\n  if (!res.flight_number) missing.push('flight number');\n  if (!res.meet_style) missing.push('meet style');\n}\nif (res.trip_type === 'HOURLY' || res.trip_type === 'ROADSHOW') {\n  if (res.hours_booked === null) missing.push('hours booked');\n}\nif (res.trip_type === 'P2P' && !res.dropoff_address) missing.push('dropoff address');\nif (!call_id) missing.push('call id');\nif (!intake_id) missing.push('intake id');\nif (!res.consent) missing.push('consent');\n\nconst slots_ok = missing.length === 0;\n\n// --- canonical hash + trip id ---------------------------------------------\nconst canonical = JSON.stringify([\n  tenant_id, intake_id, res.trip_type, res.first, res.last, res.mobile_e164,\n  res.pax_count, res.vehicle_class, res.luggage_count, res.pickup_datetime,\n  res.pickup_tz, res.pickup_address, res.dropoff_address, res.stops,\n  res.hours_booked, res.airline, res.flight_number, res.meet_style,\n  res.billing_type, res.booker_name, res.booker_company, res.special_notes, quote_id\n]);\nconst payload_hash = crypto.createHash('sha256').update(canonical).digest('hex');\n\n// AC-YYMMDD-XXXX. The suffix is derived from the payload hash, so the same reservation\n// always mints the same trip id — a retry cannot invent a second trip number.\nconst nowChi = new Date();\nconst y = String(nowChi.getUTCFullYear()).slice(2);\nconst mo = ('0' + (nowChi.getUTCMonth() + 1)).slice(-2);\nconst da = ('0' + nowChi.getUTCDate()).slice(-2);\nconst suffix = payload_hash.replace(/[^0-9a-z]/gi, '').slice(0, 4).toUpperCase();\nconst trip_id = 'AC-' + y + mo + da + '-' + suffix;\n\n// --- GHL contact custom fields (23 aic_* ids, verified live) ---------------\nconst CF = {\n  trip_type: 'qta1B3kERujMTLNDlj66', pickup_datetime: 'HOfBlSbheMOtChBuK0vU',\n  pickup_address: 'L9FS4ZqII0OvQe5KESh7', dropoff_address: 'ISMLWbZhk0V9eVHXut86',\n  stops: 'Os3wL0uOEddjnDlRdVwl', pax_count: '5mLlo7JQv1s2PTjGHfBq',\n  luggage_count: 'Yiyl1wPsGs927sT2l6KK', vehicle_class: 'OtmDBeaHItbSvOUHRTGY',\n  hours_booked: 'DKJoMbtyRJE1VPpjuM20', airline: 'FQc1deemot7Um7yawjcz',\n  flight_number: 'sLr1SgxWniXbbABjTYgu', meet_style: 'ugYKX5zQXK4UkclTYXbg',\n  special_notes: 'jSoQ7iPIZp32m4DBlV1l', booker_name: 'T485WCP9WVkbeaP9VnSd',\n  booker_company: 'uwGZ2sMwnLsNJAsRXaEY', billing_type: 'Z36xyFEoF9brVdNeKAIP',\n  intake_id: 'rG7ddiPNaCtnVb6WpQ6q', call_id: 'qPEAuGHATmSwj5ZD0zDD',\n  recording_url: 'vn8dhOTRYTLov1epE3Bf', crm_status: 'dKWoeh5ayPS7clTpAy4O',\n  payload_hash: 'kKCOH83XaTlUAdaeCxh5', consent: 'NrB5VCwkvsSsBBcVPmSx',\n  tenant: '16MIe7L45qETn9TtcOK5'\n};\n\nconst cf = [];\nconst put = (id, v) => {\n  if (v === null || v === undefined || v === '') return;\n  cf.push({ id: id, value: String(v) });\n};\nput(CF.trip_type, res.trip_type);\nput(CF.pickup_datetime, res.pickup_datetime ? (res.pickup_datetime.replace('T', ' ')) : '');\nput(CF.pickup_address, res.pickup_address);\nput(CF.dropoff_address, res.dropoff_address);\nput(CF.stops, res.stops.length ? res.stops.join(' | ') : '');\nput(CF.pax_count, res.pax_count);\nput(CF.luggage_count, res.luggage_count);\nput(CF.vehicle_class, res.vehicle_class);\nput(CF.hours_booked, res.hours_booked);\nput(CF.airline, res.airline);\nput(CF.flight_number, res.flight_number);\nput(CF.meet_style, res.meet_style);\nput(CF.special_notes, res.special_notes);\nput(CF.booker_name, res.booker_name);\nput(CF.booker_company, res.booker_company);\nput(CF.billing_type, res.billing_type);\nput(CF.intake_id, intake_id);\nput(CF.call_id, call_id);\nput(CF.recording_url, res.recording_url);\nput(CF.payload_hash, payload_hash);\nput(CF.consent, res.consent ? 'true' : 'false');\nput(CF.tenant, tenant_id);\n\n// --- OWNER RAIL LAW § 9: our own numbers never create or mutate a real lead ---\nconst OURS = String(($vars && $vars.OUR_NUMBERS) || '').replace(/[^0-9,]/g, '').split(',');\nconst digits10 = (s) => String(s || '').replace(/[^0-9]/g, '').slice(-10);\nlet is_our_number = false;\nfor (let i = 0; i < OURS.length; i++) {\n  const o = digits10(OURS[i]);\n  if (o && o === digits10(res.mobile_e164)) { is_our_number = true; break; }\n}\nconst zz = str(($vars && $vars.ZZ_TEST_CONTACT_ID) || '');\n\n// --- RUN 3 QUARANTINE RAIL -------------------------------------------------\n// THE single place WF-COMMIT decides a call is synthetic. Every suppression and\n// every TEST tag downstream reads is_test from here — same one-node shape as the\n// § 9 gate, so it cannot be edited out of one branch and left live in another.\n//\n//   is_test = NANP-reserved 555-01xx caller\n//          OR an explicit test_mode flag on the payload\n//          OR (tenant \"demo\" AND the caller is one of OUR OWN lines)\n//\n// The third clause is deliberately narrow. A REAL person dialling the demo line\n// from an unknown public number is NOT a test — the receipt is the demo, and\n// suppressing it would break the product. Only our own lines are quarantined.\n// Area code 555 is NANP-reserved and can never be a real dialable line, so the\n// prefix test is exact — no real caller is ever caught by it.\nconst d10 = digits10(res.mobile_e164);\nconst is_555 = d10.length === 10 && d10.slice(0, 3) === '555';\nconst test_mode = (args.test_mode === true || body.test_mode === true ||\n                   String(args.test_mode).toLowerCase() === 'true');\nconst is_test = !!(is_555 || test_mode || (tenant_id === 'demo' && is_our_number));\n\nlet test_reason = '';\nif (is_555) test_reason = 'caller is a NANP-reserved 555 test number';\nelse if (test_mode) test_reason = 'payload carried test_mode';\nelse if (is_test) test_reason = 'demo tenant calling from one of OUR_NUMBERS';\n\n// Tags live here, not in a node expression, so the TEST marker is version-controlled\n// and reviewable. A quarantined write is legible as synthetic in the CRM itself —\n// not only in the ledger — so nobody works it as a real lead.\nconst tags = ['aichauffeur-lead', 'aic-reservation'];\nif (is_test) { tags.push('TEST'); tags.push('zz-internal'); }\n\n// Reservation custom-object record properties (object 6a70df48cf83fcd4097d738a).\nconst properties = {\n  trip_id: trip_id, tenant_id: tenant_id, intake_id: intake_id, call_id: call_id,\n  trip_type: res.trip_type, pickup_datetime: res.pickup_datetime,\n  pickup_address: res.pickup_address, dropoff_address: res.dropoff_address,\n  stops: res.stops.join(' | '), vehicle_class: res.vehicle_class,\n  billing_type: res.billing_type, booker_name: res.booker_name,\n  booker_company: res.booker_company, special_notes: res.special_notes,\n  airline: res.airline, flight_number: res.flight_number, meet_style: res.meet_style,\n  payload_hash: payload_hash, captured_at: new Date().toISOString()\n};\n// RUN 3: the record says so itself, so a reservation is legible as synthetic without\n// cross-referencing the ledger.\nif (is_test) properties.is_test = 'TEST';\nif (quote_id) properties.quote_id = quote_id;\nif (res.pax_count !== null) properties.pax_count = res.pax_count;\nif (res.luggage_count !== null) properties.luggage_count = res.luggage_count;\nif (res.hours_booked !== null) properties.hours_booked = res.hours_booked;\n// Strip empties so the record never stores \"\" where the ticket should read NOT PROVIDED.\nconst propKeys = Object.keys(properties);\nfor (let i = 0; i < propKeys.length; i++) {\n  if (properties[propKeys[i]] === '' || properties[propKeys[i]] === null) delete properties[propKeys[i]];\n}\n\nreturn [{\n  json: {\n    auth_ok: auth_ok, auth_mode: auth_mode, auth_reason: auth_reason,\n    tenant_id: tenant_id, call_id: call_id, intake_id: intake_id,\n    retell_call_id: retell_call_id, quote_id: quote_id,\n    trip_id: trip_id, payload_hash: payload_hash, canonical: canonical,\n    slots_ok: slots_ok, missing: missing,\n    missing_sentence: missing.length ? ('Required details are missing: ' + missing.join(', ') + '.') : '',\n    res: res, cf: cf, properties: properties,\n    is_our_number: is_our_number, zz_test_contact_id: zz,\n    is_test: is_test, test_reason: test_reason, tags: tags,\n    started_ms: Date.now()\n  }\n}];\n"
      }
    },
    {
      "id": "n_authorized",
      "name": "Authorized?",
      "type": "n8n-nodes-base.if",
      "typeVersion": 2.2,
      "position": [
        -500,
        0
      ],
      "parameters": {
        "conditions": {
          "options": {
            "caseSensitive": true,
            "leftValue": "",
            "typeValidation": "loose",
            "version": 2
          },
          "conditions": [
            {
              "id": "c1",
              "leftValue": "={{ $json.auth_ok }}",
              "rightValue": "",
              "operator": {
                "type": "boolean",
                "operation": "true",
                "singleValue": true
              }
            }
          ],
          "combinator": "and"
        },
        "options": {}
      }
    },
    {
      "id": "n_respond_unauthorized",
      "name": "Respond Unauthorized",
      "type": "n8n-nodes-base.respondToWebhook",
      "typeVersion": 1.1,
      "position": [
        -300,
        200
      ],
      "parameters": {
        "respondWith": "json",
        "options": {
          "responseCode": 401
        },
        "responseBody": "={{ JSON.stringify({ trip_id: null, reservation_state: \"FAILED\", crm_write_status: \"WRITE_FAILED\", provider_reservation_id: null, duplicate: false, reason: $json.auth_reason, message: \"That request was not authorized.\" }) }}"
      }
    },
    {
      "id": "n_prior",
      "name": "Load Prior Commit",
      "type": "n8n-nodes-base.dataTable",
      "typeVersion": 1.1,
      "position": [
        -300,
        -40
      ],
      "alwaysOutputData": true,
      "onError": "continueRegularOutput",
      "parameters": {
        "resource": "row",
        "operation": "get",
        "dataTableId": {
          "__rl": true,
          "mode": "id",
          "value": "DCNqBPRtXLaLFSEh"
        },
        "returnAll": true,
        "options": {},
        "matchType": "allConditions",
        "filters": {
          "conditions": [
            {
              "keyName": "call_id",
              "condition": "eq",
              "keyValue": "={{ $(\"Verify + Normalize\").first().json.retell_call_id }}"
            }
          ]
        }
      }
    },
    {
      "id": "n_duplicate",
      "name": "Duplicate?",
      "type": "n8n-nodes-base.if",
      "typeVersion": 2.2,
      "position": [
        -100,
        -40
      ],
      "parameters": {
        "conditions": {
          "options": {
            "caseSensitive": true,
            "leftValue": "",
            "typeValidation": "loose",
            "version": 2
          },
          "conditions": [
            {
              "id": "c1",
              "leftValue": "={{ $json.call_id }}",
              "rightValue": "",
              "operator": {
                "type": "string",
                "operation": "notEmpty",
                "singleValue": true
              }
            }
          ],
          "combinator": "and"
        },
        "options": {}
      }
    },
    {
      "id": "n_build_duplicate_response",
      "name": "Build Duplicate Response",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        100,
        140
      ],
      "parameters": {
        "jsCode": "// WF-COMMIT · node \"Build Duplicate Response\"\n// A repeated call_id returns the ORIGINAL stored result verbatim. No second contact\n// upsert, no second reservation record, no second trip id. The ledger row written by\n// the first commit is the source of truth; this node only replays it.\nconst prior = $input.first().json || {};\n\nlet original = {};\ntry {\n  original = JSON.parse(prior.response_json || '{}');\n} catch (e) {\n  original = {};\n}\n\n// If the stored envelope is unreadable, rebuild the essentials from the ledger columns\n// rather than inventing a fresh commit.\nif (!original.trip_id) {\n  original = {\n    trip_id: prior.trip_id || null,\n    reservation_state: prior.reservation_state || 'REQUEST_RECEIVED',\n    crm_write_status: prior.crm_write_status || 'WRITE_UNVERIFIED',\n    provider_reservation_id: null,\n    ghl_contact_id: prior.contact_id || null,\n    reservation_record_id: prior.record_id || null,\n    quote_id: prior.quote_id || null,\n    intake_id: prior.intake_id || null,\n    call_id: prior.call_id || null,\n    tenant_id: prior.tenant_id || null,\n    reason: 'replayed from the commit ledger; the original response envelope was unreadable',\n    message: 'That trip is already on file. Reference ' + (prior.trip_id || 'on record') + '.'\n  };\n}\n\noriginal.duplicate = true;\noriginal.replayed_at = new Date().toISOString();\nif (!original.message) {\n  original.message = 'That trip is already on file. Reference ' + (original.trip_id || 'on record') + '.';\n}\n\nreturn [{ json: original }];\n"
      }
    },
    {
      "id": "n_respond_duplicate",
      "name": "Respond Duplicate",
      "type": "n8n-nodes-base.respondToWebhook",
      "typeVersion": 1.1,
      "position": [
        300,
        140
      ],
      "parameters": {
        "respondWith": "firstIncomingItem",
        "options": {
          "responseCode": 200
        }
      }
    },
    {
      "id": "n_slots_valid",
      "name": "Slots Valid?",
      "type": "n8n-nodes-base.if",
      "typeVersion": 2.2,
      "position": [
        100,
        -140
      ],
      "parameters": {
        "conditions": {
          "options": {
            "caseSensitive": true,
            "leftValue": "",
            "typeValidation": "loose",
            "version": 2
          },
          "conditions": [
            {
              "id": "c1",
              "leftValue": "={{ $(\"Verify + Normalize\").first().json.slots_ok }}",
              "rightValue": "",
              "operator": {
                "type": "boolean",
                "operation": "true",
                "singleValue": true
              }
            }
          ],
          "combinator": "and"
        },
        "options": {}
      }
    },
    {
      "id": "n_respond_validation_failed",
      "name": "Respond Validation Failed",
      "type": "n8n-nodes-base.respondToWebhook",
      "typeVersion": 1.1,
      "position": [
        300,
        40
      ],
      "parameters": {
        "respondWith": "json",
        "options": {
          "responseCode": 200
        },
        "responseBody": "={{ JSON.stringify({ trip_id: null, reservation_state: \"FAILED\", crm_write_status: \"WRITE_FAILED\", provider_reservation_id: null, duplicate: false, reason: \"required slots missing\", missing: $(\"Verify + Normalize\").first().json.missing, message: $(\"Verify + Normalize\").first().json.missing_sentence }) }}"
      }
    },
    {
      "id": "n_ghl_upsert_contact",
      "name": "GHL Upsert Contact",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.4,
      "position": [
        300,
        -240
      ],
      "credentials": {
        "httpHeaderAuth": {
          "id": "wOmBNtlzVgn2fVAc",
          "name": "GHL Header Auth"
        }
      },
      "alwaysOutputData": true,
      "onError": "continueRegularOutput",
      "parameters": {
        "method": "POST",
        "url": "https://services.leadconnectorhq.com/contacts/upsert",
        "authentication": "genericCredentialType",
        "genericAuthType": "httpHeaderAuth",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            {
              "name": "Version",
              "value": "2021-07-28"
            },
            {
              "name": "Accept",
              "value": "application/json"
            }
          ]
        },
        "options": {
          "response": {
            "response": {
              "neverError": true
            }
          },
          "timeout": 12000
        },
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ JSON.stringify({ locationId: \"sdShCZCaxce8DHKbYcIl\", firstName: $(\"Verify + Normalize\").first().json.res.first, lastName: $(\"Verify + Normalize\").first().json.res.last || undefined, phone: $(\"Verify + Normalize\").first().json.res.mobile_e164 || undefined, email: $(\"Verify + Normalize\").first().json.res.email || undefined, companyName: $(\"Verify + Normalize\").first().json.res.booker_company || undefined, source: $(\"Verify + Normalize\").first().json.is_test ? \"AI Chauffeur TEST (quarantined)\" : \"AI Chauffeur Reservation\", tags: $(\"Verify + Normalize\").first().json.tags, customFields: $(\"Verify + Normalize\").first().json.cf }) }}"
      }
    },
    {
      "id": "n_create_reservation_record",
      "name": "Create Reservation Record",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.4,
      "position": [
        500,
        -240
      ],
      "credentials": {
        "httpHeaderAuth": {
          "id": "wOmBNtlzVgn2fVAc",
          "name": "GHL Header Auth"
        }
      },
      "alwaysOutputData": true,
      "onError": "continueRegularOutput",
      "parameters": {
        "method": "POST",
        "url": "https://services.leadconnectorhq.com/objects/custom_objects.reservation/records",
        "authentication": "genericCredentialType",
        "genericAuthType": "httpHeaderAuth",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            {
              "name": "Version",
              "value": "2021-07-28"
            },
            {
              "name": "Accept",
              "value": "application/json"
            }
          ]
        },
        "options": {
          "response": {
            "response": {
              "neverError": true
            }
          },
          "timeout": 12000
        },
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ JSON.stringify({ locationId: \"sdShCZCaxce8DHKbYcIl\", properties: $(\"Verify + Normalize\").first().json.properties }) }}"
      }
    },
    {
      "id": "n_associate_to_contact",
      "name": "Associate To Contact",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.4,
      "position": [
        700,
        -240
      ],
      "credentials": {
        "httpHeaderAuth": {
          "id": "wOmBNtlzVgn2fVAc",
          "name": "GHL Header Auth"
        }
      },
      "alwaysOutputData": true,
      "onError": "continueRegularOutput",
      "parameters": {
        "method": "POST",
        "url": "https://services.leadconnectorhq.com/associations/relations",
        "authentication": "genericCredentialType",
        "genericAuthType": "httpHeaderAuth",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            {
              "name": "Version",
              "value": "2021-07-28"
            },
            {
              "name": "Accept",
              "value": "application/json"
            }
          ]
        },
        "options": {
          "response": {
            "response": {
              "neverError": true
            }
          },
          "timeout": 12000
        },
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ JSON.stringify({ locationId: \"sdShCZCaxce8DHKbYcIl\", associationId: \"6a70e026747242cebef44955\", firstRecordId: $(\"Resolve Contact\").first().json.contact_id, secondRecordId: $(\"Create Reservation Record\").first().json.record.id }) }}"
      }
    },
    {
      "id": "n_ghl_trip_note",
      "name": "GHL Trip Note",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.4,
      "position": [
        900,
        -240
      ],
      "credentials": {
        "httpHeaderAuth": {
          "id": "wOmBNtlzVgn2fVAc",
          "name": "GHL Header Auth"
        }
      },
      "alwaysOutputData": true,
      "onError": "continueRegularOutput",
      "parameters": {
        "method": "POST",
        "url": "=https://services.leadconnectorhq.com/contacts/{{ $(\"Resolve Contact\").first().json.contact_id }}/notes",
        "authentication": "genericCredentialType",
        "genericAuthType": "httpHeaderAuth",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            {
              "name": "Version",
              "value": "2021-07-28"
            },
            {
              "name": "Accept",
              "value": "application/json"
            }
          ]
        },
        "options": {
          "response": {
            "response": {
              "neverError": true
            }
          },
          "timeout": 12000
        },
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ JSON.stringify({ body: \"AI CHAUFFEUR RESERVATION \" + $(\"Verify + Normalize\").first().json.trip_id + \"\\n\" + \"Trip: \" + $(\"Verify + Normalize\").first().json.res.trip_type + \" \\u00b7 \" + $(\"Verify + Normalize\").first().json.res.vehicle_class + \"\\n\" + \"Pickup: \" + $(\"Verify + Normalize\").first().json.res.pickup_datetime + \" \\u00b7 \" + $(\"Verify + Normalize\").first().json.res.pickup_address + \"\\n\" + \"Intake: \" + $(\"Verify + Normalize\").first().json.intake_id + \" \\u00b7 Call: \" + $(\"Verify + Normalize\").first().json.call_id }) }}"
      }
    },
    {
      "id": "n_read_back_record",
      "name": "Read Back Record",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.4,
      "position": [
        1100,
        -240
      ],
      "credentials": {
        "httpHeaderAuth": {
          "id": "wOmBNtlzVgn2fVAc",
          "name": "GHL Header Auth"
        }
      },
      "alwaysOutputData": true,
      "onError": "continueRegularOutput",
      "parameters": {
        "method": "GET",
        "url": "=https://services.leadconnectorhq.com/objects/custom_objects.reservation/records/{{ $(\"Create Reservation Record\").first().json.record.id }}",
        "authentication": "genericCredentialType",
        "genericAuthType": "httpHeaderAuth",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            {
              "name": "Version",
              "value": "2021-07-28"
            },
            {
              "name": "Accept",
              "value": "application/json"
            }
          ]
        },
        "options": {
          "response": {
            "response": {
              "neverError": true
            }
          },
          "timeout": 12000
        }
      }
    },
    {
      "id": "n_verify__status",
      "name": "Verify + Status",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1300,
        -240
      ],
      "parameters": {
        "jsCode": "// WF-COMMIT · node \"Verify + Status\"\n// READ-AFTER-WRITE. The record is re-fetched from GHL and hash-compared against what we\n// intended to store. crm_write_status is decided by what the CRM actually returned, never\n// by what the write call claimed. A write that 201s but reads back wrong is UNVERIFIED.\nconst crypto = require('crypto');\n\nconst n = $('Verify + Normalize').first().json;\nconst up = $('Resolve Contact').first().json.upsert_raw || {};\nconst resolved = $('Resolve Contact').first().json || {};\nconst rec = $('Create Reservation Record').first().json || {};\nconst assoc = $('Associate To Contact').first().json || {};\nconst note = $('GHL Trip Note').first().json || {};\nconst rb = $input.first().json || {};\n\nconst bad = (r) => {\n  const sc = Number((r || {}).statusCode || (r || {}).status || 0);\n  return !!((r || {}).error || (sc && (sc < 200 || sc >= 300)));\n};\n\nconst contact_id = resolved.contact_id || ((up.contact || {}).id) || '';\nconst record_id = ((rec.record || {}).id) || rec.id || '';\nconst assoc_id = assoc.id || '';\nconst note_ok = !!((note.note && note.note.id) || note.id);\n\nconst rbRec = rb.record || {};\nconst stored = rbRec.properties || {};\n\n// --- field-by-field comparison against the intended properties -------------\nconst intended = n.properties || {};\nconst keys = Object.keys(intended);\nconst mismatches = [];\nlet compared = 0, matched = 0;\nfor (let i = 0; i < keys.length; i++) {\n  const k = keys[i];\n  if (k === 'captured_at') continue; // server clock, not a caller fact\n  compared++;\n  const want = String(intended[k] === null || intended[k] === undefined ? '' : intended[k]).trim();\n  const got = String(stored[k] === null || stored[k] === undefined ? '' : stored[k]).trim();\n  if (want === got) matched++;\n  else mismatches.push(k + ': wrote \"' + want + '\", CRM holds \"' + got + '\"');\n}\n\n// Hash both sides over the same key order so the comparison is one value, not a vibe.\nconst hashOf = (obj) => {\n  const ks = keys.slice().sort();\n  const parts = [];\n  for (let i = 0; i < ks.length; i++) {\n    if (ks[i] === 'captured_at') continue;\n    const v = obj[ks[i]];\n    parts.push(ks[i] + '=' + String(v === null || v === undefined ? '' : v).trim());\n  }\n  return crypto.createHash('sha256').update(parts.join('|')).digest('hex');\n};\nconst intended_hash = hashOf(intended);\nconst stored_hash = hashOf(stored);\nconst hash_match = intended_hash === stored_hash;\n\n// --- crm_write_status ------------------------------------------------------\n// On the zz-test path there is no upsert response to judge, so only the record matters.\nconst writeFailed = (!resolved.is_our_number && bad(up)) || bad(rec)\n                    || !contact_id || !record_id;\nlet crm_write_status;\nif (writeFailed) {\n  crm_write_status = 'WRITE_FAILED';\n} else if (bad(rb) || !rbRec.id) {\n  crm_write_status = 'WRITE_UNVERIFIED';   // wrote, but could not read it back\n} else if (hash_match && assoc_id) {\n  crm_write_status = 'CRM_VERIFIED';\n} else {\n  crm_write_status = 'WRITE_UNVERIFIED';\n}\n\n// --- reservation state policy ---------------------------------------------\n// demo tenant may say CONFIRMED, and only on CRM_VERIFIED. Every other tenant is\n// CAPTURE_ONLY and is hard-capped at REQUEST_RECEIVED no matter how clean the write was.\nconst CONFIRM_TENANTS = ['demo'];\nconst mayConfirm = CONFIRM_TENANTS.indexOf(String(n.tenant_id)) >= 0;\n\nlet reservation_state;\nif (crm_write_status === 'WRITE_FAILED') reservation_state = 'FAILED';\nelse if (crm_write_status === 'CRM_VERIFIED' && mayConfirm) reservation_state = 'CONFIRMED';\nelse reservation_state = 'REQUEST_RECEIVED';\n\nconst capped = (crm_write_status === 'CRM_VERIFIED' && !mayConfirm);\n\nlet message;\nif (reservation_state === 'CONFIRMED') {\n  message = 'Your trip is set. Reference ' + n.trip_id +\n            ' — every detail was checked against the stored record.';\n} else if (reservation_state === 'REQUEST_RECEIVED') {\n  message = 'Your request is in, reference ' + n.trip_id +\n            '. Dispatch will confirm the details with you.';\n} else {\n  message = 'I could not store that reservation. Dispatch will call you back to take it directly.';\n}\n\nconst reason = crm_write_status === 'CRM_VERIFIED'\n  ? (capped ? 'record verified; tenant is CAPTURE_ONLY so state is capped at REQUEST_RECEIVED'\n            : 'record written and verified by read-after-write hash compare')\n  : (crm_write_status === 'WRITE_FAILED'\n     ? ('write failed — contact_id=' + (contact_id || 'none') + ' record_id=' + (record_id || 'none'))\n     : ('written but not verified — ' + (mismatches.length ? mismatches.join(' · ')\n                                        : 'record could not be read back')));\n\nreturn [{\n  json: {\n    trip_id: n.trip_id,\n    reservation_state: reservation_state,\n    crm_write_status: crm_write_status,\n    provider_reservation_id: null,           // Phase 2: no provider submission yet\n    ghl_contact_id: contact_id || null,\n    reservation_record_id: record_id || null,\n    association_id: assoc_id || null,\n    note_written: note_ok,\n    quote_id: n.quote_id,\n    intake_id: n.intake_id,\n    call_id: n.call_id,\n    tenant_id: n.tenant_id,\n    payload_hash: n.payload_hash,\n    intended_hash: intended_hash,\n    stored_hash: stored_hash,\n    hash_match: hash_match,\n    fields_compared: compared,\n    fields_matched: matched,\n    mismatches: mismatches,\n    capture_only_capped: capped,\n    reason: reason,\n    message: message,\n    commit_ms: Date.now() - (n.started_ms || Date.now())\n  }\n}];\n"
      }
    },
    {
      "id": "n_build_response__ticket",
      "name": "Build Response + Ticket",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1500,
        -240
      ],
      "parameters": {
        "jsCode": "// WF-COMMIT · node \"Build Response + Ticket\"\n// Final contract for the agent, plus the dispatch ticket used by the SMS/email path.\n// A field the caller never gave prints \"— NOT PROVIDED\". It is never inferred, never\n// defaulted, and never left blank in a way that reads as \"nothing needed here\".\nconst n = $('Verify + Normalize').first().json;\nconst v = $('Verify + Status').first().json;\nconst r = n.res || {};\n\nconst NP = '— NOT PROVIDED';\nconst show = (x) => {\n  if (x === null || x === undefined) return NP;\n  const s = String(x).trim();\n  return s === '' ? NP : s;\n};\n\nconst TRIP_LABEL = {\n  HOURLY: 'Hourly · as directed', ROADSHOW: 'Roadshow',\n  AIRPORT_ARR: 'Airport arrival', P2P: 'Point to point'\n};\nconst MEET_LABEL = { CURBSIDE: 'Curbside', BAGGAGE: 'Baggage claim', PLANESIDE: 'Planeside' };\nconst BILL_LABEL = { CC_ON_FILE: 'Card on file', DIRECT_BILL: 'Direct bill', NEW_ACCOUNT: 'New account' };\n\nconst lines = [];\nlines.push('AI CHAUFFEUR — DISPATCH TICKET');\nlines.push('Trip ID        : ' + show(v.trip_id));\nlines.push('State          : ' + show(v.reservation_state));\nlines.push('CRM write      : ' + show(v.crm_write_status));\nlines.push('Trip type      : ' + (TRIP_LABEL[r.trip_type] || show(r.trip_type)) +\n           '  [' + show(r.trip_type) + ']');\nlines.push('');\nlines.push('Passenger      : ' + show((String(r.first || '') + ' ' + String(r.last || '')).trim()));\nlines.push('Mobile         : ' + show(r.mobile_e164));\nlines.push('Email          : ' + show(r.email));\nlines.push('Passengers     : ' + show(r.pax_count));\nlines.push('Luggage        : ' + show(r.luggage_count));\nlines.push('');\nlines.push('Pickup         : ' + show(r.pickup_datetime) + '  (' + show(r.pickup_tz) + ')');\nlines.push('Pickup address : ' + show(r.pickup_address));\nlines.push('Dropoff        : ' + show(r.dropoff_address));\nlines.push('Stops          : ' + show((r.stops || []).join(' | ')));\nlines.push('Hours booked   : ' + show(r.hours_booked));\nlines.push('Vehicle        : ' + show(r.vehicle_class));\nlines.push('');\nlines.push('Airline        : ' + show(r.airline));\nlines.push('Flight         : ' + show(r.flight_number));\nlines.push('Meet style     : ' + (MEET_LABEL[r.meet_style] || show(r.meet_style)));\nlines.push('');\nlines.push('Booker         : ' + show(r.booker_name) + '  /  ' + show(r.booker_company));\nlines.push('Billing        : ' + (BILL_LABEL[r.billing_type] || show(r.billing_type)));\nlines.push('Notes          : ' + show(r.special_notes));\nlines.push('');\nlines.push('Quote ID       : ' + show(v.quote_id));\nlines.push('Intake ID      : ' + show(v.intake_id));\nlines.push('Call ID        : ' + show(v.call_id));\nlines.push('Record ID      : ' + show(v.reservation_record_id));\nlines.push('Provider res.  : ' + show(v.provider_reservation_id));\nlines.push('Verify         : ' + v.fields_matched + '/' + v.fields_compared +\n           ' fields matched on read-back · hash ' + (v.hash_match ? 'match' : 'MISMATCH'));\nconst ticket_text = lines.join('\\n');\n\n// Owner SMS: status first, readable from a lock screen without opening anything.\nconst owner_sms = [\n  v.reservation_state + ' · ' + show(v.trip_id),\n  (TRIP_LABEL[r.trip_type] || show(r.trip_type)) + ' · ' + show(r.vehicle_class),\n  show(r.pickup_datetime) + ' · ' + show(r.pickup_address),\n  show((String(r.first || '') + ' ' + String(r.last || '')).trim()) + ' · ' + show(r.mobile_e164),\n  'CRM ' + v.crm_write_status\n].join('\\n');\n\nconst esc = (s) => String(s).replace(/&/g, '&amp;').replace(/</g, '&lt;').replace(/>/g, '&gt;');\nconst email_subject = v.reservation_state + ' — ' + show(v.trip_id) + ' · ' +\n                      (TRIP_LABEL[r.trip_type] || show(r.trip_type));\nconst email_html =\n  '<div style=\"background:#06080F;padding:28px 14px;font-family:Inter,Helvetica,Arial,sans-serif;\">' +\n  '<div style=\"max-width:640px;margin:0 auto;\">' +\n  '<p style=\"margin:0 0 6px;font-size:11px;letter-spacing:.14em;text-transform:uppercase;' +\n  'color:#3B82F6;font-weight:700;\">AI CHAUFFEUR · DISPATCH</p>' +\n  '<h1 style=\"margin:0 0 10px;font-size:22px;color:#EEF2FA;font-weight:800;\">' +\n  esc(v.reservation_state) + ' — ' + esc(show(v.trip_id)) + '</h1>' +\n  '<p style=\"margin:0 0 14px;font-size:13px;line-height:1.7;color:#9AA6BD;\">' +\n  esc(v.reason) + '</p>' +\n  '<pre style=\"margin:0;padding:18px;background:#0B0F1A;border:1px solid #1E293B;' +\n  'color:#EEF2FA;font-family:SFMono-Regular,Consolas,Menlo,monospace;font-size:12px;' +\n  'line-height:1.65;white-space:pre-wrap;\">' + esc(ticket_text) + '</pre>' +\n  '</div></div>';\n\nconst response = {\n  trip_id: v.trip_id,\n  is_test: !!n.is_test,\n  test_reason: n.test_reason || '',\n  reservation_state: v.reservation_state,\n  crm_write_status: v.crm_write_status,\n  provider_reservation_id: v.provider_reservation_id,\n  ghl_contact_id: v.ghl_contact_id,\n  reservation_record_id: v.reservation_record_id,\n  quote_id: v.quote_id,\n  intake_id: v.intake_id,\n  call_id: v.call_id,\n  tenant_id: v.tenant_id,\n  duplicate: false,\n  fields_matched: v.fields_matched,\n  fields_compared: v.fields_compared,\n  hash_match: v.hash_match,\n  reason: v.reason,\n  message: v.message,\n  commit_ms: v.commit_ms\n};\n\nreturn [{\n  json: Object.assign({}, response, {\n    response_json: JSON.stringify(response),\n    ticket_text: ticket_text,\n    owner_sms: owner_sms,\n    email_subject: email_subject,\n    email_html: email_html,\n    // RUN 3 QUARANTINE: a synthetic call never pages the owner. Suppression is\n    // total — no SMS, no email — and is decided by the single is_test flag minted\n    // at the ingress node, never re-derived here.\n    send_ticket: v.crm_write_status !== 'WRITE_FAILED' && !n.is_test,\n    suppressed_reason: n.is_test ? ('owner alerts suppressed — ' + (n.test_reason || 'test call')) : '',\n    caller_mobile: r.mobile_e164 || '',\n    caller_email: r.email || ''\n  })\n}];\n"
      }
    },
    {
      "id": "n_ledger",
      "name": "Write Commit Ledger",
      "type": "n8n-nodes-base.dataTable",
      "typeVersion": 1.1,
      "position": [
        1700,
        -240
      ],
      "onError": "continueRegularOutput",
      "parameters": {
        "resource": "row",
        "operation": "insert",
        "dataTableId": {
          "__rl": true,
          "mode": "id",
          "value": "DCNqBPRtXLaLFSEh"
        },
        "columns": {
          "mappingMode": "defineBelow",
          "matchingColumns": [],
          "value": {
            "tenant_id": "={{ $(\"Build Response + Ticket\").first().json.tenant_id }}",
            "call_id": "={{ $(\"Verify + Normalize\").first().json.retell_call_id }}",
            "intake_id": "={{ $(\"Build Response + Ticket\").first().json.intake_id || \"\" }}",
            "trip_id": "={{ $(\"Build Response + Ticket\").first().json.trip_id || \"\" }}",
            "quote_id": "={{ $(\"Build Response + Ticket\").first().json.quote_id || \"\" }}",
            "record_id": "={{ $(\"Build Response + Ticket\").first().json.reservation_record_id || \"\" }}",
            "contact_id": "={{ $(\"Build Response + Ticket\").first().json.ghl_contact_id || \"\" }}",
            "payload_hash": "={{ $(\"Verify + Normalize\").first().json.payload_hash }}",
            "crm_write_status": "={{ $(\"Build Response + Ticket\").first().json.crm_write_status }}",
            "reservation_state": "={{ $(\"Build Response + Ticket\").first().json.reservation_state }}",
            "response_json": "={{ $(\"Build Response + Ticket\").first().json.response_json }}",
            "created_at": "={{ $now.toISO() }}",
            "is_test": "={{ $(\"Verify + Normalize\").first().json.is_test ? \"TEST\" : \"\" }}"
          }
        },
        "options": {}
      }
    },
    {
      "id": "n_respond_commit",
      "name": "Respond Commit",
      "type": "n8n-nodes-base.respondToWebhook",
      "typeVersion": 1.1,
      "position": [
        1900,
        -240
      ],
      "parameters": {
        "respondWith": "json",
        "responseBody": "={{ $(\"Build Response + Ticket\").first().json.response_json }}",
        "options": {
          "responseCode": 200
        }
      }
    },
    {
      "id": "n_send_ticket",
      "name": "Send Ticket?",
      "type": "n8n-nodes-base.if",
      "typeVersion": 2.2,
      "position": [
        2100,
        -240
      ],
      "parameters": {
        "conditions": {
          "options": {
            "caseSensitive": true,
            "leftValue": "",
            "typeValidation": "loose",
            "version": 2
          },
          "conditions": [
            {
              "id": "c1",
              "leftValue": "={{ $(\"Build Response + Ticket\").first().json.send_ticket }}",
              "rightValue": "",
              "operator": {
                "type": "boolean",
                "operation": "true",
                "singleValue": true
              }
            }
          ],
          "combinator": "and"
        },
        "options": {}
      }
    },
    {
      "id": "n_ticket_owner_sms",
      "name": "Ticket Owner SMS",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.4,
      "position": [
        2300,
        -320
      ],
      "credentials": {
        "httpHeaderAuth": {
          "id": "wOmBNtlzVgn2fVAc",
          "name": "GHL Header Auth"
        }
      },
      "alwaysOutputData": true,
      "onError": "continueRegularOutput",
      "parameters": {
        "method": "POST",
        "url": "https://services.leadconnectorhq.com/conversations/messages",
        "authentication": "genericCredentialType",
        "genericAuthType": "httpHeaderAuth",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            {
              "name": "Version",
              "value": "2021-07-28"
            },
            {
              "name": "Accept",
              "value": "application/json"
            }
          ]
        },
        "options": {
          "response": {
            "response": {
              "neverError": true
            }
          },
          "timeout": 12000
        },
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ JSON.stringify({ type: \"SMS\", contactId: $vars.OWNER_ALERT_CONTACT_ID, fromNumber: $vars.OWNER_SMS_FROM, message: $(\"Build Response + Ticket\").first().json.owner_sms }) }}"
      }
    },
    {
      "id": "n_ticket_owner_email",
      "name": "Ticket Owner Email",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.4,
      "position": [
        2500,
        -320
      ],
      "credentials": {
        "httpHeaderAuth": {
          "id": "wOmBNtlzVgn2fVAc",
          "name": "GHL Header Auth"
        }
      },
      "alwaysOutputData": true,
      "onError": "continueRegularOutput",
      "parameters": {
        "method": "POST",
        "url": "https://services.leadconnectorhq.com/conversations/messages",
        "authentication": "genericCredentialType",
        "genericAuthType": "httpHeaderAuth",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            {
              "name": "Version",
              "value": "2021-07-28"
            },
            {
              "name": "Accept",
              "value": "application/json"
            }
          ]
        },
        "options": {
          "response": {
            "response": {
              "neverError": true
            }
          },
          "timeout": 12000
        },
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ JSON.stringify({ type: \"Email\", contactId: $vars.OWNER_ALERT_CONTACT_ID, subject: $(\"Build Response + Ticket\").first().json.email_subject, html: $(\"Build Response + Ticket\").first().json.email_html }) }}"
      }
    },
    {
      "id": "n_ournum",
      "name": "Our Number?",
      "type": "n8n-nodes-base.if",
      "typeVersion": 2.2,
      "position": [
        280,
        -380
      ],
      "parameters": {
        "conditions": {
          "options": {
            "caseSensitive": true,
            "leftValue": "",
            "typeValidation": "loose",
            "version": 2
          },
          "conditions": [
            {
              "id": "c_our",
              "leftValue": "={{ $(\"Verify + Normalize\").first().json.is_our_number }}",
              "rightValue": "",
              "operator": {
                "type": "boolean",
                "operation": "true",
                "singleValue": true
              }
            }
          ],
          "combinator": "and"
        },
        "options": {}
      }
    },
    {
      "id": "n_zz",
      "name": "Load ZZ Test Contact",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.4,
      "position": [
        480,
        -440
      ],
      "credentials": {
        "httpHeaderAuth": {
          "id": "wOmBNtlzVgn2fVAc",
          "name": "GHL Header Auth"
        }
      },
      "alwaysOutputData": true,
      "onError": "continueRegularOutput",
      "parameters": {
        "method": "GET",
        "url": "=https://services.leadconnectorhq.com/contacts/{{ $vars.ZZ_TEST_CONTACT_ID }}",
        "authentication": "genericCredentialType",
        "genericAuthType": "httpHeaderAuth",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            {
              "name": "Version",
              "value": "2021-07-28"
            },
            {
              "name": "Accept",
              "value": "application/json"
            }
          ]
        },
        "options": {
          "timeout": 12000,
          "response": {
            "response": {
              "neverError": true
            }
          }
        }
      }
    },
    {
      "id": "n_resolve",
      "name": "Resolve Contact",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        680,
        -380
      ],
      "parameters": {
        "jsCode": "// WF-COMMIT · node \"Resolve Contact\"\n// § 9 OWNER RAIL LAW enforcement point. Downstream nodes read the contact id from HERE,\n// never from the upsert node directly, so the guard cannot be bypassed by a later edit.\n//\n// A call placed from one of OUR OWN numbers (every Retell line, the published SMS line,\n// both owner cells — n8n Variable OUR_NUMBERS) must never create or mutate a real lead.\n// On that path the upsert is skipped entirely and the reservation is associated to the\n// dedicated zz-test contact instead.\nconst n = $('Verify + Normalize').first().json;\nconst inbound = $input.first().json || {};\n\nlet contact_id = '';\nlet route = '';\n\nif (n.is_our_number) {\n  contact_id = n.zz_test_contact_id || '';\n  route = 'zz-test — caller is one of OUR_NUMBERS, upsert skipped (§ 9)';\n} else {\n  contact_id = ((inbound.contact || {}).id) || '';\n  route = 'caller upsert';\n}\n\nreturn [{\n  json: {\n    contact_id: contact_id,\n    route: route,\n    is_our_number: !!n.is_our_number,\n    upsert_raw: inbound\n  }\n}];\n"
      }
    }
  ],
  "connections": {
    "Commit Webhook": {
      "main": [
        [
          {
            "node": "Verify + Normalize",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Verify + Normalize": {
      "main": [
        [
          {
            "node": "Authorized?",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Authorized?": {
      "main": [
        [
          {
            "node": "Load Prior Commit",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "Respond Unauthorized",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Load Prior Commit": {
      "main": [
        [
          {
            "node": "Duplicate?",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Duplicate?": {
      "main": [
        [
          {
            "node": "Build Duplicate Response",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "Slots Valid?",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Build Duplicate Response": {
      "main": [
        [
          {
            "node": "Respond Duplicate",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Slots Valid?": {
      "main": [
        [
          {
            "node": "Our Number?",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "Respond Validation Failed",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "GHL Upsert Contact": {
      "main": [
        [
          {
            "node": "Resolve Contact",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Create Reservation Record": {
      "main": [
        [
          {
            "node": "Associate To Contact",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Associate To Contact": {
      "main": [
        [
          {
            "node": "GHL Trip Note",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "GHL Trip Note": {
      "main": [
        [
          {
            "node": "Read Back Record",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Read Back Record": {
      "main": [
        [
          {
            "node": "Verify + Status",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Verify + Status": {
      "main": [
        [
          {
            "node": "Build Response + Ticket",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Build Response + Ticket": {
      "main": [
        [
          {
            "node": "Write Commit Ledger",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Write Commit Ledger": {
      "main": [
        [
          {
            "node": "Respond Commit",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Respond Commit": {
      "main": [
        [
          {
            "node": "Send Ticket?",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Send Ticket?": {
      "main": [
        [
          {
            "node": "Ticket Owner SMS",
            "type": "main",
            "index": 0
          }
        ],
        []
      ]
    },
    "Ticket Owner SMS": {
      "main": [
        [
          {
            "node": "Ticket Owner Email",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Our Number?": {
      "main": [
        [
          {
            "node": "Load ZZ Test Contact",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "GHL Upsert Contact",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Load ZZ Test Contact": {
      "main": [
        [
          {
            "node": "Resolve Contact",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Resolve Contact": {
      "main": [
        [
          {
            "node": "Create Reservation Record",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "settings": {
    "executionOrder": "v1",
    "errorWorkflow": "SlnAeMrVRORsF0w7",
    "saveDataErrorExecution": "all",
    "saveDataSuccessExecution": "all"
  }
}